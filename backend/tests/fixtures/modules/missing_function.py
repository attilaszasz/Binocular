"""Test fixture module missing the required check_firmware function."""

from __future__ import annotations

MODULE_VERSION: str = "1.0.0"
SUPPORTED_DEVICE_TYPE: str = "test_devices"

# Deliberately missing check_firmware function
