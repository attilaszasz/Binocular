# DATA INTEGRITY: Device-Module Linking & Refactor
**Created**: 2026-06-04 | **Feature**: [spec.md](../spec.md)

## Migration Safety — Backup, Rollback, Atomicity

- [X] CHK001 Does the spec require that a pre-migration backup snapshot is created before migration 007 executes, and is failure to create the backup a hard stop? [Survivability, Spec §6.2] <!-- Evaluator: Resolved — added FR-009 to spec.md (backup creation hard-stop, verification, rollback procedure) -->
- [X] CHK002 Is the migration's transactional boundary defined — are all seven steps wrapped in a single `BEGIN IMMEDIATE … COMMIT` so that any step failure rolls back the entire migration? [Atomicity, Spec §4 FR-003] <!-- Evaluator: Covered by plan.md HINT-001 and data-model.md §4.3 (atomic BEGIN IMMEDIATE) -->
- [X] CHK003 Is the partial-failure scenario (e.g., backfill succeeds but DROP TABLE fails) addressed with explicit rollback behavior, or does the spec assume the transaction handles it? [Recoverability, Plan §HINT-001] <!-- Evaluator: Covered by plan.md HINT-001 (transaction handles partial failure via atomicity) -->
- [X] CHK004 Is the exact rollback procedure documented (restore backup snapshot, restart application) in a requirement or risk mitigation, not just an implementation note? [Recoverability, Spec §6.2] <!-- Evaluator: Resolved — added to FR-009 in spec.md (restore backup snapshot, restart application) -->
- [X] CHK005 Does the spec require verification that the backup snapshot is valid and complete before any destructive operations (DROP COLUMN, DROP TABLE) are executed? [Survivability, Spec §4 FR-003] <!-- Evaluator: Resolved — added to FR-009 in spec.md (verified backup before destructive ops) -->
- [X] CHK006 Is the `schema_version` table's role in preventing re-application of migration 007 after a successful run explicitly required, and does a rollback restore the correct pre-007 schema version? [Recoverability, Spec §6.2] <!-- Evaluator: Resolved — added to FR-009 and FR-010 in spec.md (schema_version prevents re-application, restored by backup) -->
- [X] CHK007 Is there an explicit justification for forward-only migrations (no DOWN script), and does it cover the scenario where an operator needs to undo migration 007 without a backup? [Completeness, Spec §6.1 bullet 1] <!-- Evaluator: Resolved — added FR-010 to spec.md (forward-only constraint, backup required for undo) -->
- [X] CHK008 Does the spec state what happens if migration 007 is applied against a database that already has `module_id` on `devices` (idempotency / re-entrancy requirement)? [Survivability, Spec §4 FR-003] <!-- Evaluator: Resolved — added FR-010 to spec.md and edge case (schema_version tracking prevents re-application) -->

## FK Integrity — `module_id` References, Orphan Handling

- [X] CHK009 Is `PRAGMA foreign_keys = ON` required both during migration execution and at application runtime, and is this requirement explicit rather than assumed from existing code? [Correctness, Spec §5] <!-- Evaluator: Resolved — added FR-011 to spec.md (PRAGMA foreign_keys = ON required) -->
- [X] CHK010 Does the spec require that device creation/update with a non-existent `moduleId` string is rejected at the application layer with a specific error code, not solely relying on database-level FK constraint violations? [Completeness, Spec §4 FR-007] <!-- Evaluator: Resolved — added FR-012 to spec.md (module_not_found distinct error code) -->
- [X] CHK011 Is there a requirement that module deletion MUST unlink all referencing devices (SET `module_id = NULL`) before the `DELETE FROM modules` statement, covering every referencing table? [Correctness, Spec §4 FR-008] <!-- Evaluator: Covered by FR-008 (extended) and data-model.md §5.3 (unlink before DELETE) -->
- [X] CHK012 Is the race condition between unlinking devices and deleting the module addressed — specifically, is the unlinking and module deletion wrapped in a single transaction? [Consistency, Spec §2.3 edge case "Module is deleted"] <!-- Evaluator: Resolved — added to FR-008 in spec.md (single transaction wraps unlink+delete) -->
- [X] CHK013 Does the spec explicitly state whether `ON DELETE CASCADE` or `ON DELETE SET NULL` is defined on the FK constraint, and document why application-level handling was chosen instead? [Completeness, Spec §5] <!-- Evaluator: Resolved — added to FR-008 in spec.md (no ON DELETE CASCADE/SET NULL; application-level rationale) -->
- [X] CHK014 Is the distinction between `modules.id` (integer PK, the FK target) and `modules.module_id` (string, the public identifier) clear in every requirement, API contract, and UI specification to prevent implementation confusion? [Clarity, Spec §6.1 bullet 4] <!-- Evaluator: Covered by spec.md Assumptions, contracts/inventory-api.md, data-model.md -->
- [X] CHK015 Are all valid states where `module_id` can be NULL explicitly enumerated (post-migration backfill failure, post-module deletion) and differentiated from invalid NULL states (creation without selecting a module)? [Completeness, Spec §2.3] <!-- Evaluator: Covered by spec.md §2.3 edge cases + data-model.md §5.2 Transition Rules table -->
- [X] CHK016 Is there a requirement that the `modules.id` integer PK is stable across module updates/reinstalls, ensuring that existing device FK references are not broken by module lifecycle operations? [Correctness, Spec §2.2 excluded scope] <!-- Evaluator: Resolved — added to spec.md Assumptions (modules.id PK is stable auto-increment) -->

## Data Consistency — Derived Device Type After JOIN Change

- [X] CHK017 Does the spec require that device type is always derived at query time via JOIN with `modules.display_name` and never materialized/stored on the device row, to prevent stale-cache drift? [Consistency, Spec §4 FR-002] <!-- Evaluator: Covered by spec.md FR-002 (derive from display_name at query time via JOIN) -->
- [X] CHK018 Does the query design require `LEFT JOIN` (not `INNER JOIN`) so that devices with NULL `module_id` are not silently excluded from inventory results? [Completeness, Spec §3.4 US4] <!-- Evaluator: Covered by spec.md US4 AS1 + data-model.md §6.1 (LEFT JOIN with COALESCE) -->
- [X] CHK019 Is the derived device type value for unlinked devices specified consistently as `"Unlinked"` across all query paths, API responses, and UI displays? [Consistency, Spec §8 SC-004] <!-- Evaluator: Covered by spec.md SC-004, contracts/inventory-api.md, data-model.md (consistent 'Unlinked') -->
- [X] CHK020 Does the spec require that a module `display_name` rename propagates to all linked device displays on the next query without requiring a migration, cache invalidation, or manual sync? [Consistency, Spec §3.2 US2 AS3] <!-- Evaluator: Covered by spec.md US2 AS3 + Edge Cases (display_name renames propagate immediately) -->
- [X] CHK021 Is the new index `idx_devices_active_module_name` correctly aligned with the inventory listing query that filters by `is_archived` and joins on `module_id`? [Correctness, Spec §4 FR-004] <!-- Evaluator: Covered by data-model.md §4.2 migration step 5 (index on is_archived, module_id, name) -->
- [X] CHK022 Is the inventory grouping logic specified to handle NULL `module_id` keys correctly — sorting them last and grouping them under a single "Unlinked" header rather than creating one group per NULL row? [Consistency, Spec §3.3 US3 AS2] <!-- Evaluator: Covered by spec.md US3 AS3 + data-model.md §6.3 (single 'Unlinked' group, sorted last) -->
- [X] CHK023 After clearing `device_type_schedules`, is the scheduler's behavior when `list_schedules()` returns an empty list defined — does it safely idle with zero jobs rather than crashing or looping? [Consistency, Spec §4 FR-003] <!-- Evaluator: Resolved — added FR-013 to spec.md (scheduler safely idles with empty list) -->

## Unlinked Device Handling — NULL `module_id` Requirements

- [X] CHK024 Is there a requirement that unlinked devices (NULL `module_id`) MUST appear in the inventory view and MUST NOT be silently hidden or filtered out? [Completeness, Spec §3.4 US4 AS1] <!-- Evaluator: Covered by spec.md US4 AS1 (unlinked devices must appear with status indicator) -->
- [X] CHK025 Is the `"Unlinked"` label specified consistently as the device type string in the API response's `deviceType` field, the inventory group header `name` field, and the frontend UI? [Consistency, Spec §8 SC-004] <!-- Evaluator: Covered by spec.md SC-004 + contracts/inventory-api.md (consistent 'Unlinked' label) -->
- [X] CHK026 Does the spec require a clear reassignment path for unlinked devices — specifically, that the edit form must allow selecting a module to transition from unlinked to linked? [Completeness, Spec §4 FR-006] <!-- Evaluator: Covered by spec.md FR-006 + SC-004 (reassign via edit form) -->
- [X] CHK027 Is device creation with a missing or null-equivalent `moduleId` explicitly rejected at the application layer with an error distinct from "module not found" or "module not valid"? [Correctness, Spec §3.1 US1 AS4] <!-- Evaluator: Covered by spec.md US1 AS4 + contracts/inventory-api.md §5.2 (module_id_required error) -->
- [X] CHK028 Is the scenario of zero valid installed modules addressed for both device creation (blocked with guidance) and inventory viewing (all existing devices appear as unlinked)? [Completeness, Spec §2.3 edge case "no valid installed modules"] <!-- Evaluator: Resolved — added to spec.md Edge Cases (inventory shows 'Unlinked' group when no modules) -->
- [X] CHK029 Are archived devices' `module_id` values preserved (not NULLed) so that unarchiving a device restores its original module link without requiring operator reassignment? [Consistency, Spec §2.3 (no mention of NULLing on archive)] <!-- Evaluator: Resolved — added FR-014 to spec.md (archive preserves module_id) -->
- [X] CHK030 Is the count of devices unlinked during a module deletion operation tracked and reported to the operator, or is silent unlinking acceptable per the requirements? [Completeness, Spec §6.2 risk "Module deletion breaks device links"] <!-- Evaluator: Resolved — added to FR-008 in spec.md (unlinked device count MUST be logged) -->

## Schema Evolution — Forward-Only Migration Guarantees

- [X] CHK031 Is the forward-only migration design constraint (no DOWN scripts, no in-place reversion) explicitly stated as a requirement, and does it prohibit any reversible operations within migration 007? [Completeness, Spec §7 signal "MIGRATION"] <!-- Evaluator: Resolved — added FR-010 to spec.md (forward-only constraint, no DOWN scripts) -->
- [X] CHK032 Does the migration step ordering prevent dependency violations — specifically, are schedule rows cleared before `device_types` is dropped, and is the `device_type_id` column dropped before the index that references it? [Correctness, Spec §4 FR-003] <!-- Evaluator: Covered by spec.md FR-003 operation order + data-model.md §4.2 (steps 3→4, 6→7) -->
- [X] CHK033 Is the decision to empty (not drop) the `device_type_schedules` table documented with a forward-looking rationale that future epics will repurpose or replace it, preventing a later migration from failing on a missing table? [Completeness, Spec §2.1 Included] <!-- Evaluator: Resolved — added to spec.md Assumptions (table retained for future per-module scheduling) -->
- [X] CHK034 Is there a requirement that no other tables hold FK references to `device_types` before the `DROP TABLE device_types` statement executes, and is this verified or guaranteed by the migration step order? [Correctness, Spec §6.2 risk "Schedule FK migration conflicts"] <!-- Evaluator: Resolved — added to spec.md Risks (schedule rows cleared before DROP TABLE, FR-009 backup) -->
- [X] CHK035 Is migration 007 designed to be idempotent — if applied twice (e.g., after a partial failure and restore), will it succeed rather than failing on `ALTER TABLE ... ADD COLUMN` that already exists? [Survivability, Spec §4 FR-003] <!-- Evaluator: Resolved — added FR-010 to spec.md (idempotency at runner layer, not SQL) -->
- [X] CHK036 Is there a requirement to test migration 007 against a database containing real pre-existing device data (not just an empty schema), covering both matchable and unmatchable device type names? [Testability, Spec §8 SC-005] <!-- Evaluator: Covered by spec.md SC-005 (migration tested against real data, both matchable/unmatchable) -->
- [X] CHK037 Is the breaking API change from `deviceType` to `moduleId` explicitly documented with a consumer migration guide, including the semantic change that the field now references a module identifier rather than accepting any free-text string? [Completeness, Spec §7 signal "BREAKING-CHANGE"] <!-- Evaluator: Covered by spec.md §7 BREAKING-CHANGE signal + contracts/inventory-api.md §6 -->
- [X] CHK038 Does the migration handle orphaned `device_type_id` values on existing device rows (references to non-existent `device_types` rows) gracefully during the backfill step? [Survivability, Spec §2.3 edge case "no matching module"] <!-- Evaluator: Covered by spec.md §2.3 edge case + data-model.md §4.2 (backfill JOIN handles orphaned naturally) -->
- [X] CHK039 Is the data volume assumption (~5–50 devices) reflected in migration performance expectations, and is there an upper bound beyond which the migration's in-transaction duration may be problematic? [Completeness, Spec §6.1 bullet 3] <!-- Evaluator: Resolved — added to spec.md Assumptions (5–50 devices, sub-second in-transaction duration) -->
- [X] CHK040 Is the integrity of the `schema_version` table preserved through the migration and rollback process — specifically, does restoring the backup also restore the correct schema version state? [Recoverability, Spec §6.2] <!-- Evaluator: Resolved — added to FR-009 in spec.md (schema_version restored with backup) -->
