# Testing: Official Panasonic Lumix Lenses Module
**Created**: 2026-06-06 | **Feature**: [spec.md](../spec.md)

## Fixture Coverage

- [X] CHK001 [Completeness] [Spec §FR-001, Plan §Testing Strategy] Does the lens fixture (`panasonic_firmware_index.html`) contain representative entries for both L-mount (S-*) and Micro Four Thirds (H-*) lens types, covering at least one model from each lens family? <!-- Evaluator: Covered by spec.md FR-001 (both mount types), SC-001 (zero false results from fixture), and plan.md Fixture Content Requirements -->
- [X] CHK002 [Completeness] [Spec §FR-001, Plan §Source Code Structure] Does the fixture HTML capture the actual page structure from `index5.html` — including the firmware table, `OpenWin`/`OpenWinS` JavaScript handler blocks, and version/date columns — rather than a hand-crafted synthetic page? <!-- Evaluator: Covered by plan.md Source Code Structure (captured page snapshot) and Fixture Content Requirements -->
- [X] CHK003 [Completeness] [Spec §Edge Cases, Plan §Testing Strategy] Is the unparseable fixture (`unparseable.html`) genuinely missing the firmware table structure that the module's parser depends on, so that `firmware_index_not_found` is a deterministic outcome? <!-- Evaluator: Covered by spec.md Edge Cases (unparseable → firmware_index_not_found) and plan.md Fixture Content Requirements -->
- [X] CHK004 [Completeness] [Spec §Clarifications (model matching), Plan §AD-004] Does the fixture include at least one row that matches the lens model regex (`^[SH]-[A-Z0-9]+$`) but is not a real lens (e.g., a service part or accessory row with an S- prefix), to verify the module does not produce false positives? <!-- Evaluator: Resolved — added Fixture Content Requirements to plan.md mandating false-positive test row -->
- [X] CHK005 [Completeness] [Spec §Scope (Excluded), Plan §Risk Mitigation] Does the fixture contain non-lens entries (e.g., LEICA, SIGMA, or camera body rows like DC-*/DMC-*) to confirm the module correctly ignores rows outside its defined scope? <!-- Evaluator: Resolved — added Fixture Content Requirements to plan.md mandating non-lens entries in fixture -->
- [X] CHK006 [Completeness] [Spec §FR-002, Clarifications] Are there fixture entries with varying `firmware_date` formats (e.g., with and without the "NEW" label, different date separators) to exercise the date cleaning function across representative real-world inputs? <!-- Evaluator: Resolved — added Fixture Content Requirements to plan.md mandating varying date formats -->
- [X] CHK007 [Completeness] [Spec §FR-005, Plan §AD-003] Does the fixture include at least one lens entry that has an `OpenWinS` handler (L-mount) and one that has an `OpenWin` handler (MFT) to verify download URL extraction for both JavaScript function variants? <!-- Evaluator: Covered by spec.md FR-005 (both handler types), plan.md AD-003, and Fixture Content Requirements -->
- [X] CHK008 [Completeness] [Spec §Edge Cases (no download handler), STF-001] Does the fixture include a lens entry with no `OpenWin`/`OpenWinS` handler at all, to serve as a golden input for testing the `download_url_not_found` error path? <!-- Evaluator: Covered by spec.md STF-001 and plan.md Fixture Content Requirements -->

## Failure Mode Testing

- [X] CHK009 [Completeness] [Spec §FR-004, Plan §Error Handling Strategy] Is each of the five defined `error_type` values (`product_not_found`, `firmware_index_not_found`, `firmware_not_available`, `download_url_not_found`, `firmware_page_unavailable`) exercised by at least one dedicated test case? <!-- Evaluator: Covered by plan.md Error Handling Strategy (all 5 error types enumerated) and Requirement Coverage Map -->
- [X] CHK010 [Completeness] [Spec §Edge Cases, FR-004] Is the `firmware_not_available` error path tested with the boundary conditions defined in STF-005 — an empty version cell, a whitespace-only version cell, and a non-numeric/placeholder version cell? <!-- Evaluator: Covered by spec.md STF-005 (explicit boundary conditions) and plan.md Error Handling Strategy -->
- [X] CHK011 [Completeness] [Spec §FR-004, Plan §HINT-016] Is the catch-all / unrecognized failure path tested, confirming that an unexpected internal error (e.g., a regex engine exception from malformed HTML) produces a visible `ModuleCheckResult(status="failed")` rather than an unhandled exception? <!-- Evaluator: Covered by plan.md HINT-016 (exhaustive error types, catch-all must produce visible failure) -->
- [X] CHK012 [Completeness] [Spec §Edge Cases (HTTP error/timeout), Plan §HINT-010] Is the `firmware_page_unavailable` error path tested with a FakeScrapeClient that raises each of the ScrapeClient exception types (`RobotsDeniedError`, `ScrapeTimeoutError`, `ScrapeTransportError`, `RetryExhaustedError`)? <!-- Evaluator: Covered by plan.md HINT-010 (all 4 ScrapeClient exception types enumerated) -->
- [X] CHK013 [Clarity] [Spec §FR-004, Plan §Error Handling Strategy] For each failure test, are both the `status` (must be `"failed"`) and the `error_type` in `diagnostics` asserted, and is `detail` verified to be a non-empty string? <!-- Evaluator: Covered by plan.md Error Handling Strategy (all scenarios specify error_type, detail, and diagnostics) and spec.md FR-004 -->
- [X] CHK014 [Completeness] [Spec §FR-004, Plan §HINT-011] Does every failure test verify that the `detail` field contains only a human-readable message with no raw HTML, page source fragments, or internal HTTP headers? <!-- Evaluator: Covered by plan.md HINT-011 (detail is human-readable only; HTTP diagnostics segregated to diagnostics dict) -->
- [X] CHK015 [Completeness] [Spec §Edge Cases (empty response), Plan §Risk Mitigation] Is the `firmware_index_not_found` path tested with an empty or zero-length HTTP response body (HTTP 200 with no content)? <!-- Evaluator: Resolved — added empty/zero-length response body row to plan.md Error Handling Strategy -->
- [X] CHK016 [Completeness] [Spec §FR-004, Plan §HINT-009] For the `product_not_found` and `firmware_not_available` error paths, is `source_url` in the result set to the firmware index page URL rather than being `None` or malformed? <!-- Evaluator: Covered by plan.md HINT-009 (source_url must be valid URL across all code paths) -->

## Edge Cases

- [X] CHK017 [Completeness] [Spec §Clarifications (model matching), Plan §AD-004] Are model matching edge cases tested — including case-insensitive input (e.g., `"s-r1635"` vs `"S-R1635"`), input with extra whitespace, and input with mixed alphanumeric/dash formatting? <!-- Evaluator: Covered by spec.md Clarifications (case-insensitive, strip non-alphanumeric) and plan.md AD-004 -->
- [X] CHK018 [Completeness] [Spec §Clarifications (non-alphanumeric strip), Plan §AD-004] Is the model normalization (stripping non-alphanumeric characters) tested with inputs containing punctuation, dashes, spaces, and underscores that surround a valid lens model code? <!-- Evaluator: Covered by spec.md Clarifications (strip non-alphanumeric) -->
- [X] CHK019 [Completeness] [Spec §Scope (Excluded), Plan §AD-004] Is the model regex boundary tested to confirm that camera body models (e.g., `DC-GH7`, `DMC-G9`) are rejected by the lens module and do not silently match as lenses? <!-- Evaluator: Covered by spec.md US1 AC3 (DC-GH7), plan.md AD-004 and HINT-003 (distinct camera body regex) -->
- [X] CHK020 [Completeness] [Spec §STF-003, Plan §HINT-006] Is the parsing logic tested against a page containing an order of magnitude more entries than the expected ~90 (e.g., a synthetic fixture with 500+ rows) to verify that performance degrades linearly rather than catastrophically? <!-- Evaluator: Covered by spec.md STF-003 (no additional module-level bounds needed; ScrapeClient bounds page size via response buffer + 10s timeout) -->
- [X] CHK021 [Completeness] [Spec §Clarifications (no /‑delimited variants), Plan §HINT-004] Since lens models lack `/`‑delimited grouped variants (unlike camera models), is there a test confirming that a lens model with a slash in the input (e.g., `"S-R1635/S-R1636"`) does not accidentally match a grouped pattern? <!-- Evaluator: Covered by spec.md Clarifications (lens models have no / delimited variants) and plan.md HINT-004 (_model_aliases not needed) -->
- [X] CHK022 [Completeness] [Spec §FR-002, Plan §HINT-008] Is firmware date extraction tested with edge cases such as a missing date cell, a date cell containing only the `"NEW"` label, and a date cell with Unicode or non‑ASCII characters? <!-- Evaluator: Resolved — extended plan.md HINT-008 to mandate testing of missing date cell, "NEW"-only cell, and Unicode/non-ASCII date cell -->
- [X] CHK023 [Completeness] [Spec §STF-004, Plan §AD-001] Is concurrent `check_firmware` invocation tested (e.g., by running two async checks simultaneously with a FakeScrapeClient) to verify no shared mutable state causes cross‑invocation interference? <!-- Evaluator: Covered by spec.md STF-004 (concurrent invocation handled safely) and plan.md HINT-001 -->
- [X] CHK024 [Completeness] [Spec §FR-001, Plan §HINT-006] Is the row‑level regex tested against HTML that contains deeply nested tables, malformed `<tr>` tags, or missing closing tags to verify the non‑greedy patterns do not trigger catastrophic backtracking? <!-- Evaluator: Covered by plan.md HINT-006 (non-greedy regex, bounded by ScrapeClient buffer and 10s timeout) -->
- [X] CHK025 [Completeness] [Spec §FR-005, Plan §HINT-012] When `check_input.source_url` is overridden to a non‑Panasonic origin (e.g., `https://evil.example.com/index5.html`), is there a test confirming the override is rejected and the default Panasonic URL is used instead? <!-- Evaluator: Covered by plan.md HINT-012 (source_url override constrained to Panasonic origin) -->
- [X] CHK026 [Completeness] [Spec §Clarifications (mandatory), Plan §Error Handling Strategy] Are the `diagnostics` fields in successful results verified to contain only the documented keys (`model`, `module_id`, `product_model`, `aliases`, `firmware_date`) and no unexpected or internal‑state fields? <!-- Evaluator: Covered by plan.md HINT-015 (allowed diagnostics fields enumerated; unexpected fields prohibited) -->

## Golden Tests

- [X] CHK027 [Consistency] [Spec §SC-001, US1] Is there a golden test for at least one L‑mount lens (S‑*) that asserts the exact expected `latest_version`, `status: "success"`, and `source_url` value against the captured fixture? <!-- Evaluator: Covered by spec.md US1 AC1 (S-E2470 → latest_version: "2.0", status: "success") and plan.md Golden Test Assertions -->
- [X] CHK028 [Consistency] [Spec §SC-001, US1] Is there a golden test for at least one MFT lens (H‑*) that asserts the exact expected `latest_version`, `status: "success"`, and `source_url` value against the captured fixture? <!-- Evaluator: Covered by spec.md US1 AC2 (H-ES12035 → latest_version: "1.1", status: "success") and plan.md Golden Test Assertions -->
- [X] CHK029 [Completeness] [Spec §SC-001, US1] Do all golden tests assert that `compare_versions(current_version, result.latest_version).is_newer` is `True` when the fixture version is genuinely newer than the input current version? <!-- Evaluator: Resolved — added Golden Test Assertions to plan.md mandating compare_versions().is_newer assertion -->
- [X] CHK030 [Completeness] [Spec §SC-002, FR-005] For lens entries in the fixture that have an associated download handler, does the golden test assert that `source_url` is a valid, well‑formed URL (as opposed to an empty string or a JavaScript function name)? <!-- Evaluator: Covered by spec.md SC-002, plan.md HINT-007, HINT-009, and Golden Test Assertions -->
- [X] CHK031 [Completeness] [Spec §FR-002, Plan §HINT-008] Do the golden tests assert that `result.diagnostics["firmware_date"]` is a non‑empty string for entries where the fixture contains a date? <!-- Evaluator: Covered by plan.md HINT-008 (firmware_date extraction mandatory) and Golden Test Assertions -->
- [X] CHK032 [Consistency] [Spec §US1 AC1, US1 AC2] Are the golden tests exact (asserting specific version strings like `"2.0"` and `"1.1"`) rather than loose (e.g., only checking that version is non‑empty or numeric)? <!-- Evaluator: Covered by spec.md US1 AC1/AC2 (exact version assertions) and plan.md Golden Test Assertions (exact latest_version required) -->
- [X] CHK033 [Completeness] [Spec §FR-008, Plan §Testing Strategy] Are all golden tests fully offline — with the ScrapeClient dependency replaced by a `FakeScrapeClient` that injects fixture HTML, and with zero live HTTP calls made during test execution? <!-- Evaluator: Covered by spec.md FR-008 and plan.md Testing Strategy (Golden tier uses FakeScrapeClient; HTTP boundary mocked) and HINT-001 -->
- [X] CHK034 [Completeness] [Spec §SC-003, US2 AC1] Is the unparseable fixture golden test exact, asserting `status: "failed"`, `error_type: "firmware_index_not_found"`, and a non‑empty `detail` message? <!-- Evaluator: Covered by spec.md SC-003 and US2 AC1 (exact failure assertions) -->
- [X] CHK035 [Completeness] [Spec §SC-004, US2 AC2] Is the non‑lens model golden test exact, asserting `status: "failed"` and `error_type: "product_not_found"` for a model that is definitively not a Panasonic lens? <!-- Evaluator: Covered by spec.md SC-004 and US2 AC2 (exact failure assertions) -->

## Contract Compliance

- [X] CHK036 [Completeness] [Spec §US3, Plan §Testing Strategy] Is there a contract‑load test that uses `ModuleLoader` to verify the module loads without errors, `module_id` matches `"official.panasonic_lumix_lenses"`, and `display_name` matches `"Panasonic Lumix Lenses"`? <!-- Evaluator: Covered by plan.md Testing Strategy (Integration tier: ModuleLoader loads module, validates MODULE_METADATA) -->
- [X] CHK037 [Completeness] [Spec §FR-006, Plan §Constraint] Is there a source‑code string check in the test suite that verifies no banned HTTP import patterns (`import httpx`, `from httpx`, `import requests`, `from urllib.request`) appear in the module source? <!-- Evaluator: Covered by plan.md Testing Strategy (Security tier: no direct HTTP imports via source string check) -->
- [X] CHK038 [Consistency] [Spec §Clarifications (export symbols), Plan §AD-001] Is the module's public API surface tested to confirm it exports exactly the required symbols — `MODULE_METADATA` dict and `async def check_firmware(check_input, scrape_client)` — matching the cameras module (E020) contract? <!-- Evaluator: Covered by spec.md Clarifications (match cameras module exactly) and plan.md AD-001 -->
- [X] CHK039 [Completeness] [Spec §FR-008, Plan §Testing Strategy] Does the test suite cover the parsing helpers (`parse_firmware_entries`, `extract_latest_version`) as synchronous unit tests in addition to the async `check_firmware` integration tests? <!-- Evaluator: Covered by plan.md Testing Strategy (Unit tier: parsing logic including parse_firmware_entries, find_firmware_entry, model regex matching) -->
- [X] CHK040 [Completeness] [Spec §FR-003, US3 AC3] Is there a dry‑run integration test that invokes `check_firmware` with a `FakeScrapeClient` against the fixture and asserts that no direct HTTP connections bypass the provided scrape client? <!-- Evaluator: Covered by spec.md SC-006 and US3 AC3, and plan.md Requirement Coverage Map (SC-006 → check_firmware integration test with FakeScrapeClient + fixture) -->
- [X] CHK041 [Completeness] [Spec §FR-007, Plan §Brownfield Notes] Is there a test (or existing test suite coverage) that verifies the module file is auto‑discovered by the seeder when placed in `binocular/official_modules/` and that seeding does not fail or produce duplicates? <!-- Evaluator: Resolved — added Seeder Auto-Discovery Test requirement to plan.md Testing Strategy -->
- [X] CHK042 [Consistency] [Spec §PLAN §HINT-013] Is there a test verifying that `MODULE_METADATA.version` is a non‑empty, parseable version string, enabling regression correlation between parsing logic changes and specific module releases? <!-- Evaluator: Resolved — added MODULE_METADATA Version Test requirement to plan.md Testing Strategy -->
- [X] CHK043 [Completeness] [Spec §PLAN §HINT-014] Is there a test or static check confirming the module source contains no `os.environ`, `os.getenv`, or environment‑variable reads, consistent with the stateless scraper design? <!-- Evaluator: Covered by plan.md HINT-014 (os.environ/os.getenv expressly forbidden) and Testing Strategy Security tier -->
- [X] CHK044 [Completeness] [Spec §PLAN §HINT-017] Is there a test verifying that all values in the `diagnostics` dict of a `_failed` result are JSON‑serializable (str, int, float, bool, None), with no bytes or non‑serializable objects present? <!-- Evaluator: Covered by plan.md HINT-017 (_failed helper must ensure JSON-serializable diagnostics values) -->
