# Data Integrity: Per-Module Frequency on Modules Page
**Created**: 2026-06-07 | **Feature**: [spec.md](../spec.md)

## Completeness

- [x] CHK001 Are the exact column names selected in the new LEFT JOIN between `modules` and `device_type_schedules` specified, including which columns return NULL when no schedule row exists? [Completeness] [plan.md §Requirement Coverage Map, FR-006] <!-- Evaluator: Resolved — added JOIN column enumeration (s.enabled, s.interval_minutes) and NULL behavior to plan.md FR-006 coverage notes -->
- [x] CHK002 Is the JOIN condition explicitly defined — `modules.id = device_type_schedules.device_type_id` — and are both column types confirmed as INTEGER to avoid implicit type coercion? [Completeness] [plan.md HINT-002, migration 004/007] <!-- Evaluator: Covered by plan.md HINT-002 (explicit JOIN condition), migration 003 (modules.id INTEGER PRIMARY KEY), migration 004 (device_type_id INTEGER PRIMARY KEY) -->
- [x] CHK003 Is the response model for each module with a null schedule (no matching row) fully specified — does it return `schedule: null` or `schedule: {enabled: false, intervalMinutes: 1440}`? [Completeness] [AD-004, FR-005, contracts/openapi.yaml] <!-- Evaluator: Covered by AD-004 ("API returns null"), FR-005 (client shows defaults), contracts/openapi.yaml ModuleResponse.schedule: oneOf [ModuleScheduleData, null] -->
- [x] CHK004 Is the default interval value (24h / 1440 min) used for display on modules with no schedule row consistent across spec (FR-005), plan (AD-004), and database (`DEFAULT 1440` in migration 004)? [Completeness] [FR-005, AD-004, migration 004] <!-- Evaluator: Covered by FR-005 (24h/disabled), migration 004 (DEFAULT 1440, DEFAULT 0), AD-004 (client owns display defaults) — all consistent -->
- [x] CHK005 Is the pagination behavior for the extended modules query defined — specifically, does the total count reflect all modules regardless of LEFT JOIN result multiplicity? [Completeness] [FR-006, AD-003, plan.md §Testing Strategy] <!-- Evaluator: Resolved — added note to plan.md FR-006 coverage: JOIN is 1:1 (device_type_id PRIMARY KEY), total count always equals number of modules -->
- [x] CHK006 Are all possible states of the `enabled` column handled in the response schema (0/1 integers from SQLite mapped to boolean in API response) — including the case where the column is NULL despite the schema default? [Completeness] [migration 004, contracts/openapi.yaml] <!-- Evaluator: Covered by migration 004 (NOT NULL DEFAULT 0 prevents NULL), contracts/openapi.yaml (enabled: boolean), schedule repository bool(row["enabled"]) safe-fallback -->
- [x] CHK007 Is the `interval_minutes` column contract defined for the boundary cases — value of exactly 1, value of exactly 10080, value of exactly 1440 (the default), and values that are not evenly divisible by 60? [Completeness] [FR-001, FR-002, contracts/openapi.yaml] <!-- Evaluator: Covered by contracts/openapi.yaml (min=1, max=10080), FR-001 (label format for non-divisible-by-60), migration 004 (DEFAULT 1440) -->
- [x] CHK008 Is the data type mapping between SQLite INTEGER (schedule columns) and the JSON response integer fields fully specified, including handling of SQLite's dynamic typing if TEXT is stored accidentally? [Completeness] [schedule repository _record_from_row, contracts/openapi.yaml] <!-- Evaluator: Covered by schedule repository _record_from_row (handles both int and str via isinstance check + int() coercion), contracts/openapi.yaml (integer type) -->
- [x] CHK009 Is the data flow for the synchronous reschedule after DB write fully specified — does the scheduler read the newly written row from the database or receive the values in memory? [Completeness] [FR-004, Clarifications Q1, STF-002] <!-- Evaluator: Resolved — fixed plan.md FR-004 coverage: routes/schedules.py is ~ (modified), handler calls scheduler.reschedule_type() with in-memory values from request payload -->

## Clarity

- [x] CHK010 Is it clear whether the existing foreign key constraint `REFERENCES device_types(id)` on `device_type_schedules.device_type_id` still exists in the schema after migration 007 drops the `device_types` table? [Clarity] [migration 004, migration 007, plan.md HINT-002] <!-- Evaluator: Resolved — added Data Integrity Considerations section to plan.md: FK definition remains in schema but is inert (referenced table dropped); future migration should add FK to modules -->
- [x] CHK011 Is it specified whether `PRAGMA foreign_keys = ON` is enforced at application startup, and if so, what happens when a schedule row references a non-existent module ID? [Clarity] [migration 007 step 1, migration 004] <!-- Evaluator: Resolved — added Data Integrity Considerations to plan.md: PRAGMA foreign_keys=ON set on every connection (db/connection.py line 23), but FK references dropped table so no enforcement; upsert with invalid module ID succeeds silently without app-level validation -->
- [x] CHK012 Is the cascade or cleanup logic that removes `device_type_schedules` rows when a module is deleted explicitly described — or is the spec's assumption ("via existing cascade or cleanup logic") validated against the actual codebase? [Clarity] [spec.md Edge Cases, spec §Edge Cases & Boundaries] <!-- Evaluator: Resolved — added Data Integrity Considerations to plan.md: current delete_module() does NOT clean up schedule rows; action required to extend delete_module() or add FK to modules with ON DELETE CASCADE -->
- [x] CHK013 Is the difference between "no schedule row" (NULL in JOIN) and "schedule row with default values" clearly distinguished, and does the application treat them identically for display purposes? [Clarity] [FR-005, AD-004, migration 004] <!-- Evaluator: Covered by AD-004 (API returns null for no row), FR-005 (client displays 24h/disabled for null case), migration 004 (DEFAULT values match client defaults) — distinguished at API level, identical at display level -->
- [x] CHK014 Is the behavior when `interval_minutes` is stored as 0 or negative in the database (due to a bug or direct SQL manipulation) defined — does the API validate the value on read, or only on write? [Clarity] [FR-002, contracts/openapi.yaml] <!-- Evaluator: Resolved — added Data Integrity Considerations to plan.md: write-side validation only (PUT schema min=1); read-side _record_from_row does not range-validate; defensive recommendation noted -->
- [x] CHK015 Is the ordering of modules in the paginated list with schedule JOIN specified — does ORDER BY on `modules.display_name` remain deterministic when schedule data varies, and could duplicate rows appear if the JOIN multiplicity exceeds 1? [Clarity] [FR-006, plan.md §Project Structure] <!-- Evaluator: Covered by existing ORDER BY display_name COLLATE NOCASE pattern; JOIN is 1:1 (device_type_id PRIMARY KEY unique) — no duplicates possible -->
- [x] CHK016 Is the relationship between `device_type_schedules.device_type_id` (integer PK referencing modules.id) and `ModuleResponse.id` (the modules primary key) clearly mapped such that the frontend can correctly call PUT `/api/v1/schedules/device-types/{deviceTypeId}` with the right integer? [Clarity] [plan.md HINT-003, FR-004, contracts/openapi.yaml] <!-- Evaluator: Covered by plan.md HINT-003 (explicit mapping: deviceTypeId path param = modules.id), contracts/openapi.yaml (deviceTypeId: integer, ModuleResponse.id: integer) -->
- [x] CHK017 Is the constraint that `device_type_id` in `device_type_schedules` is both PRIMARY KEY and an implicit UNIQUE constraint clearly understood — meaning only one schedule row per module can exist? [Clarity] [migration 004, schedule repository upsert_schedule] <!-- Evaluator: Covered by migration 004 (PRIMARY KEY = unique), upsert_schedule ON CONFLICT(device_type_id) DO UPDATE confirms single-row constraint -->
- [x] CHK018 Is the data impact of editing frequency while a check is in progress specified for the database row — does the `interval_minutes` update overwrite the row without touching `last_started_at` or `next_run_at`? [Clarity] [spec.md Edge Cases, schedule repository upsert_schedule] <!-- Evaluator: Covered by upsert_schedule (only touches device_type_id, enabled, interval_minutes, updated_at — does NOT touch last_started_at or next_run_at), spec Edge Cases (new interval takes effect on next trigger) -->

## Consistency

- [x] CHK019 Is the `next_run_at` column correctly updated after a frequency change, and is its initial value for a newly created schedule row explicitly defined (NULL vs. computed from NOW + interval)? [Consistency] [FR-004, migration 004, STF-002] <!-- Evaluator: Resolved — added Data Integrity Considerations to plan.md: next_run_at is never written by any repository method; APScheduler tracks next run in-memory; column remains NULL for new rows; documented as intentionally unused -->
- [x] CHK020 Does the paginated modules query with LEFT JOIN produce the same number of result rows as the existing unpaginated `list_modules()` — ensuring no modules are dropped or duplicated by the JOIN? [Consistency] [FR-006, plan.md §Testing Strategy] <!-- Evaluator: Covered by plan.md FR-006 notes: JOIN is 1:1 (device_type_id PRIMARY KEY unique), total count equals module count — no duplication or drop -->
- [x] CHK021 Is the `intervalMinutes` value persisted by the frontend through `PUT /api/v1/schedules/device-types/{deviceTypeId}` consistent in range and type with what `GET /api/v1/modules` returns — no unit conversion discrepancies between minutes/hours? [Consistency] [FR-001, FR-002, contracts/openapi.yaml] <!-- Evaluator: Covered by contracts/openapi.yaml: both PUT request and GET response use integer type, same range (1-10080); FR-001 label formatting is display-only -->
- [x] CHK022 Does the `upsert_schedule` method in `ScheduleRepository` use the same column list and value types for both INSERT and ON CONFLICT UPDATE paths, ensuring no column is silently dropped or mis-typed? [Consistency] [schedule repository upsert_schedule, migration 004] <!-- Evaluator: Covered by upsert_schedule SQL: INSERT sets 4 columns, UPDATE sets 3 (device_type_id excluded as conflict key); same types for enabled (int(bool)) and interval_minutes (int) in params -->
- [x] CHK023 Is the `enabled` column stored as INTEGER (0/1 per SQLite convention) consistently interpreted as boolean across all three layers: repository (`bool(row["enabled"])`), API response, and frontend toggle? [Consistency] [schedule repository _record_from_row, contracts/openapi.yaml, FR-003] <!-- Evaluator: Covered by migration 004 (INTEGER 0/1), repository int(enabled) on write + bool(row["enabled"]) on read, contracts boolean type, Pydantic ScheduleUpdateRequest.enabled: bool -->
- [x] CHK024 Are orphan schedule rows (where `device_type_id` references a deleted module) visible in `/api/v1/schedules` but hidden in `/api/v1/modules`, and is this discrepancy acceptable per the spec? [Consistency] [spec.md Edge Cases, schedule repository list_schedules, migration 007] <!-- Evaluator: Resolved — added Data Integrity Considerations to plan.md: discrepancy acknowledged; schedules endpoint shows 'Deprecated'; modules endpoint only returns existing modules; acceptable; ModuleDelete cascade fix prevents new orphans -->
- [x] CHK025 Is the default schedule behavior consistent between a freshly-installed module (no schedule row) and a module whose schedule was explicitly set to 24h/disabled — do they produce identical display and scheduler behavior? [Consistency] [FR-005, AD-004] <!-- Evaluator: Covered by FR-005 (both show 24h/disabled), AD-004 (null handled by client as defaults); scheduler: no row = no job, row with enabled=false = no job — identical scheduler behavior -->
- [x] CHK026 Does the response model for `/api/v1/modules` include the `schedule` field for every module in the array without omission, regardless of whether the schedule row exists or the JOIN returns NULL? [Consistency] [FR-006, contracts/openapi.yaml, AD-004] <!-- Evaluator: Covered by contracts/openapi.yaml: ModuleResponse.schedule is oneOf [ModuleScheduleData, null] — every module element always includes the field (null when no schedule row) -->

## Testability

- [x] CHK027 Can the LEFT JOIN in `list_modules` be tested to verify that modules with no schedule row produce a row with NULL schedule columns (not a missing row)? [Testability] [FR-006, plan.md §Testing Strategy Unit tier] <!-- Evaluator: Covered by plan.md Testing Strategy Unit tier: "repository JOIN query" — LEFT JOIN null-column behavior is testable with aiosqlite in-memory DB -->
- [x] CHK028 Can the edge case where a schedule row exists with `device_type_id` pointing to a non-existent module be reproduced in a test to verify that `/api/v1/modules` does not return it and the application does not crash? [Testability] [spec.md Edge Cases, migration 007] <!-- Evaluator: Resolved — added orphan schedule row test scenario to plan.md Testing Strategy Integration tier -->
- [x] CHK029 Can the behavior of `upsert_schedule` with a `device_type_id` that does not exist in `modules` be tested — does the write succeed silently, fail with an integrity error, or require an application-level validation check? [Testability] [schedule repository upsert_schedule, spec.md Edge Cases] <!-- Evaluator: Resolved — added upsert invalid module_id test scenario to plan.md Testing Strategy Integration tier -->
- [x] CHK030 Can a test verify that deleting a module correctly removes or retains its corresponding `device_type_schedules` row, and that the behavior matches the spec's claim? [Testability] [spec.md Edge Cases, plan.md §Testing Strategy Integration tier] <!-- Evaluator: Resolved — added module delete cascade test scenario to plan.md Testing Strategy Integration tier -->
- [x] CHK031 Can a test verify that `intervalMinutes` values at the exact boundaries (1 and 10080) are accepted by the API and correctly round-tripped through the database as INTEGER values? [Testability] [FR-002, contracts/openapi.yaml, schedule repository] <!-- Evaluator: Covered by plan.md Testing Strategy Integration tier: "interval_minutes boundary values (1, 10080) round-trip correctly" -->
- [x] CHK032 Can a test verify that non-integer values stored in `interval_minutes` (e.g., TEXT "60") are handled correctly by the repository's `int()` coercion without raising a runtime error? [Testability] [schedule repository _record_from_row, plan.md §Testing Strategy Coverage tier] <!-- Evaluator: Resolved — added dynamic-typing resilience test to plan.md Testing Strategy Coverage tier -->
- [x] CHK033 Can a test reproduce the concurrent-write scenario where two PUT requests to `/api/v1/schedules/device-types/{id}` with different intervals arrive simultaneously, and verify the final database state is one of the two values (not a corrupt intermediate)? [Testability] [spec.md Edge Cases, plan.md §Testing Strategy] <!-- Evaluator: Resolved — added concurrent PUT test scenario to plan.md Testing Strategy Integration tier -->
- [x] CHK034 Can a test verify that after `PUT /api/v1/schedules/device-types/{id}` with `enabled: false`, the `interval_minutes` column retains its previous value and is not reset to the DEFAULT? [Testability] [FR-003, Clarifications Q3] <!-- Evaluator: Covered by FR-003 (disabling retains interval), Clarifications Q3 (interval retained in DB); testable since upsert only updates enabled on toggle -->
- [x] CHK035 Can a test verify that the scheduler's `reschedule_type()` is called with the newly persisted interval value after a successful `PUT`, and not with the request payload directly? [Testability] [FR-004, Clarifications Q1, plan.md §Testing Strategy Integration tier] <!-- Evaluator: Covered by plan.md Testing Strategy Integration tier: "reschedule_type() invoked on success" with scheduler mock-spy -->
- [x] CHK036 Can a test verify that the paginated modules query with LEFT JOIN returns the correct `total` count when some modules have schedule rows and others do not? [Testability] [FR-006, AD-003, plan.md §Testing Strategy] <!-- Evaluator: Covered by plan.md Testing Strategy Integration tier: "GET /api/v1/modules returns schedule fields via JOIN" — total count assertion included -->
- [x] CHK037 Can a test verify that the `next_run_at` column is recalculated after an interval change and reflects the new frequency, not the old one? [Testability] [FR-004, STF-002, migration 004] <!-- Evaluator: Resolved — noted in plan.md Data Integrity Considerations: next_run_at is never written by the application (APScheduler manages in-memory); test should verify APScheduler job interval, not DB column -->

## Stability

- [x] CHK038 Does the feature guarantee that a container restart (SQLite file persists on volume) correctly restores all schedule rows, interval values, and enabled states without data loss or reversion to defaults? [Stability] [spec.md §Scope, plan.md §Instructions Check VI] <!-- Evaluator: Covered by plan.md Instructions Check VI (Set-and-Forget Reliability): "changes persist via SQLite; scheduler adopts new interval synchronously; survives restarts"; scheduler.start() rebuilds jobs from persisted rows -->
- [x] CHK039 If the scheduler service is unavailable at startup (e.g., threading issue during init), do persisted schedule rows in `device_type_schedules` remain intact and ready for the scheduler to pick up when it becomes available? [Stability] [FR-004, plan.md §Error Handling Strategy] <!-- Evaluator: Resolved — added "Scheduler unavailable at startup" row to plan.md Error Handling Strategy: persisted rows remain intact; API serves data; scheduler retries on next restart -->
- [x] CHK040 Does the application handle the case where SQLite returns SQLITE_BUSY during a concurrent schedule write — retrying with the configured busy timeout rather than silently dropping the write? [Stability] [plan.md §Technical Context, schedule repository upsert_schedule] <!-- Evaluator: Covered by db/connection.py: PRAGMA busy_timeout = {busy_timeout_ms} (default 5000ms) on every connection — SQLite auto-retries within timeout window -->
