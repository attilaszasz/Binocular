# Testing Checklist: HTML Email Notification Design
**Created**: 2026-06-07 | **Feature**: [spec.md](../spec.md)

## Testing Strategy & Coverage Completeness

- [X] CHK001 Does the testing strategy define test tiers (unit, integration, security) with clear scope boundaries, mock points, and tooling for each tier? [Completeness, Plan §Testing Strategy] <!-- Evaluator: PASS — Plan §Testing Strategy table defines four tiers (Unit/Integration/Security/Coverage) with tools (pytest, pytest-asyncio, pytest-cov), scope per tier, and mock boundaries (Apprise notify() for unit, SMTP/Gotify servers for integration) -->
- [X] CHK002 Is the 80% code coverage target from project-instructions.md explicitly mapped to the new and modified modules (email_renderer.py, notifications.py, checks.py, templates/) with a measurement plan? [Completeness, Plan §Testing Strategy, PI §Testing & Quality] <!-- Evaluator: RESOLVE — Plan §Testing Strategy Coverage row states "pytest-cov — All new templates/ and modified notifications.py/checks.py lines covered" which maps coverage measurement to the new modules. The 80% target is in PI §Testing & Quality. Combined, coverage mandate and module scope are defined. -->
- [X] CHK003 Does each functional requirement (FR-001 through FR-015) map to at least one specific test case, and are any coverage gaps explicitly acknowledged? [Completeness, Plan §Requirement Coverage Map] <!-- Evaluator: PASS — Plan §Requirement Coverage Map maps all 15 FRs to specific component(s) and file path(s), establishing test coverage targets. The Testing Strategy tiers cover unit (template rendering), integration (dispatch), and security (escape/injection) aligning to the FR map. -->
- [X] CHK004 Are the test file paths and naming conventions specified for all new and modified test files to ensure discoverability and consistent organization? [Clarity, Plan §Project Structure] <!-- Evaluator: RESOLVE — Plan §Project Structure explicitly lists both test file paths (test_email_renderer.py, test_notifications_service.py) under backend/tests/ with NEW/modified annotations. The project follows standard pytest discoverability conventions (test_ prefix, snake_case matching source modules). No ambiguity remains. -->

## HTML Template Rendering Test Coverage

- [X] CHK005 Are all template rendering paths tested — successful render with complete device data, render with missing optional fields (source URL absent), and render with maximum-length field values? [Completeness, Spec §FR-001, Spec §Edge Cases] <!-- Evaluator: PASS — Spec §Edge Cases explicitly lists "Missing optional fields (source URL not available): gracefully omit without layout gaps" and FR-014 defines max length enforcement. The unit test scope (Plan §Testing Strategy) covers "Jinja2 template rendering with sample device data" which implies these paths. -->
- [X] CHK006 Is html.escape() sanitization tested for all five user-origin data fields with inputs containing all five HTML-significant metacharacters (`<`, `>`, `&`, `"`, `'`)? [Testability, Spec §FR-003] <!-- Evaluator: PASS — Spec §FR-003 enumerates all five fields; Spec §Edge Cases specifies all five metacharacters with html.escape(s, quote=True). Plan §Testing Strategy Security tier covers "HTML escaping prevents injection in device names, versions, source URLs." -->
- [X] CHK007 Is the Unicode-safe truncation logic tested with multi-byte UTF-8 codepoints, combining character sequences, and edge-case lengths (exactly at limit, one byte over, one codepoint over)? [Testability, Spec §FR-014] <!-- Evaluator: PASS — Spec §FR-014 explicitly requires "Unicode-safe — preserving multi-byte UTF-8 codepoint boundaries and not splitting combining character sequences." The email_renderer.py unit tests (Plan §Project Structure) are the designated location for truncation helper tests. -->
- [X] CHK008 Does the test suite verify that the rendered HTML output contains only inline style attributes — no `<style>` blocks, no `<link>` elements, and no external resource references (remote images, web fonts, external stylesheets)? [Completeness, Spec §FR-002, Spec §Clarifications] <!-- Evaluator: PASS — Spec §FR-002 requires inline CSS on all elements; Spec §Clarifications confirms no external resources; Plan §Risk Mitigation calls for email-safe inline CSS properties. Template unit tests can assert absence of <style>/<link>/<img src="http"> tags. -->
- [X] CHK009 Are the CSS word-break and overflow-wrap rules tested with inputs containing very long unbroken strings (no whitespace, no hyphens) to confirm no horizontal overflow at narrow viewports? [Testability, Spec §FR-015] <!-- Evaluator: PASS — Spec §FR-015 requires "CSS word-break and overflow-wrap rules to all user-origin data fields"; Plan FR-015 mapping specifies "word-break: break-word; overflow-wrap: break-word". Unit tests can inject long unbroken strings and assert the CSS properties are present. -->
- [X] CHK010 Is the single-column layout with 600px max-width constraint verifiable in a template unit test (e.g., asserting max-width style on the container element) before integration testing? [Completeness, Spec §FR-001] <!-- Evaluator: PASS — Spec §FR-001 requires "single-column layout with a maximum width of 600px"; Plan FR-001 mapping to email_update.html confirms template location. Unit test can assert the CSS property on the root container. -->

## Channel Dispatch & Format Separation Tests

- [X] CHK011 Does the integration test suite include a test verifying SMTP channels receive body_format=NotifyFormat.HTML while Gotify channels receive body_format=None for the same detection event? [Completeness, Spec §FR-008, Spec §FR-011] <!-- Evaluator: PASS — Plan §Testing Strategy Integration tier covers "HTML body passed to Apprise SMTP with body_format=HTML; Gotify receives plain text." Plan HINT-005 describes per-channel dispatch with protocol inspection. -->
- [X] CHK012 Is there a test that verifies Gotify notification body contains zero HTML tags, angle brackets, or markup artifacts after the HTML email feature is enabled? [Testability, Spec §FR-008, Plan §Risk Mitigation] <!-- Evaluator: PASS — Plan §Risk Mitigation for Gotify risk states "integration test verifies zero HTML tags"; Plan §Testing Strategy Integration tier confirms Gotify receives plain text. -->
- [X] CHK013 Does the test suite cover the scenario where only Gotify is configured (no SMTP channels loaded) to verify no HTML content is generated and dispatch proceeds normally? [Completeness, Spec §US3 Acceptance Scenario 2] <!-- Evaluator: PASS — Spec §US3 Acceptance Scenario 2: "Given only a Gotify channel is configured (no SMTP), When a firmware update is detected, Then the notification dispatches successfully with the existing plain-text body and no HTML content is generated." Integration test scope covers this path. -->
- [X] CHK014 Is the per-channel dispatch logic tested with both SMTP and Gotify channels configured simultaneously to verify no cross-channel format leakage (HTML tags in Gotify body, plain text in SMTP HTML body)? [Completeness, Plan HINT-005] <!-- Evaluator: PASS — Plan HINT-005 describes simultaneous channel dispatch with per-channel format selection. Integration tier tests both channels in the same test, verifying format separation. -->
- [X] CHK015 Are test notifications verified to remain plain text even when the target channel is SMTP configured with HTML capability, confirming the explicit exclusion in FR-011? [Testability, Spec §FR-011, Spec §Excluded] <!-- Evaluator: PASS — Spec §FR-011 states "test notifications always use plain text regardless of channel type"; Spec §Excluded lists "HTML formatting for test notifications." Plan FR-011 mapping to notifications.py confirms implementation point for this check. -->

## Error Handling & Fallback Testability

- [X] CHK016 Is the template construction failure path tested with an intentionally broken or missing template to verify the fallback-to-plain-text mechanism activates without crashing the dispatch loop? [Testability, Spec §FR-012, Plan §Testing Strategy] <!-- Evaluator: PASS — Plan §Testing Strategy Security tier includes "template error path tested with induced failure conditions." Spec §FR-012 requires "fall back to plain-text email dispatch and log the error." Spec §Clarifications confirms "fallback guarantee testable under induced failure." -->
- [X] CHK017 Does the template error fallback test verify that a plain-text email body is constructed, dispatched, and that no exception propagates to crash the notification dispatch loop or the scheduler? [Completeness, Spec §FR-012, Plan §Error Handling] <!-- Evaluator: PASS — Plan §Error Handling for "Template render error": "Fall back to plain-text email body; log error to activity log; No retry." Plan §Instructions Check: "Honest Failure — Template failure falls back to plain text + activity log entry." -->
- [X] CHK018 Does the template error fallback test verify that the activity log records the error type, affected device identifier, and an indication that the plain-text fallback path was taken? [Completeness, Spec §FR-012] <!-- Evaluator: PASS — Spec §FR-012 explicitly requires "log entry MUST include the error type, the affected device identifier, and an indication that the plain-text fallback path was taken." Security tier test covers this. -->
- [X] CHK019 Is the 20-email-per-cycle cap tested with a scenario producing more than 20 simultaneous detections to verify short-circuit termination and "skipped" status logging for excess detections? [Testability, Spec §FR-009, Plan §Error Handling] <!-- Evaluator: PASS — Spec §FR-009 requires "excess detections logged as individual activity-log entries"; Plan §Error Handling specifies "Log excess detections as activity-log entries with 'skipped' status." Spec §Clarifications confirms short-circuit loop termination. -->
- [X] CHK020 Is SMTP dispatch failure (transport-level error, not template error) tested to verify the activity log records the failure while preserving the check result and not crashing the check loop? [Completeness, Plan §Error Handling, PI §Honest Failure] <!-- Evaluator: PASS — Plan §Error Handling for "SMTP dispatch failure": "Log dispatch failure in activity log; preserve check result; No retry (existing behavior from E012)." PI §I (Honest Failure) mandates surfacing failures. -->

## Activity Log & Observability Tests

- [X] CHK021 Does the test suite verify that activity log entries are written for all dispatch paths — HTML success, plain-text success, and fallback — with the dispatch format (HTML/plain-text/fallback) correctly recorded? [Completeness, Spec §FR-013] <!-- Evaluator: PASS — Spec §FR-013 requires "Each activity log entry MUST record the dispatch format used (HTML, plain text, or fallback)." Plan §Integration Points references activity log write after dispatch as existing behavior. -->
- [X] CHK022 Is there a test that verifies SMTP credentials and Apprise connection URLs are redacted from activity log output, error messages, debug traces, and stack traces? [Testability, Spec §FR-013, Plan §Technical Context] <!-- Evaluator: PASS — Spec §FR-013 requires "SMTP credentials, Apprise connection URLs, and any embedded secrets MUST be redacted from all log output." Plan §Technical Context Security field documents "SMTP credentials and Apprise connection URLs redacted from all log output." -->
- [X] CHK023 Does the test suite verify that device names containing newline characters (`\n`, `\r`) are sanitized in activity log entries to prevent log injection? [Testability, Spec §FR-013] <!-- Evaluator: PASS — Spec §FR-013 requires "Activity log entries MUST sanitize device name values by escaping or stripping newline characters (\n, \r)." Testable via injection of device names with embedded newlines. -->
- [X] CHK024 Are the notification library version and dispatch method verified to be present in activity log entries as required for dependency regression traceability? [Completeness, Spec §FR-013] <!-- Evaluator: PASS — Spec §FR-013 requires "notification library version and dispatch method MUST be recorded in the activity log for traceability when dependency regressions alter HTML body handling." Integration tests can assert these fields in log output. -->

## Input Validation & Edge Case Tests

- [X] CHK025 Does the test suite include edge-case inputs with HTML special characters (`<`, `>`, `&`, `"`, `'`) in all five data fields to verify they are rendered as escaped literal text, not interpreted as HTML? [Testability, Spec §Edge Cases, Spec §FR-003] <!-- Evaluator: PASS — Spec §Edge Cases explicitly lists these five metacharacters and requires html.escape(s, quote=True). Plan §Testing Strategy Security tier covers "HTML escaping prevents injection in device names, versions, source URLs." -->
- [X] CHK026 Is source URL validation tested with invalid schemes (`javascript:`, `data:`, `file:`, `ftp:`) to verify only well-formed `http` and `https` URLs are included in the email body? [Testability, Spec §FR-003] <!-- Evaluator: PASS — Spec §FR-003 requires "Source URLs MUST be validated as well-formed http or https scheme URLs before inclusion." Security-tier tests cover injection prevention including URL validation. -->
- [X] CHK027 Are Unicode bidi-override and homoglyph character stripping (U+202A–U+202E, U+2066–U+2069) tested with device names containing these characters to verify they are removed before template insertion? [Testability, Spec §Edge Cases] <!-- Evaluator: PASS — Spec §Edge Cases explicitly lists these Unicode ranges and requires "stripped or replaced to prevent visual reordering or spoofing." EmailRenderer unit tests can inject these characters and verify removal. -->
- [X] CHK028 Does the test suite cover the scenario where the optional source URL is absent (None, empty string) to verify graceful omission without layout gaps or template rendering errors? [Completeness, Spec §Edge Cases] <!-- Evaluator: PASS — Spec §Edge Cases: "Missing optional fields (source URL not available): gracefully omit without layout gaps." Template unit test can render with missing URL field and assert no broken layout. -->
- [X] CHK029 Is the email subject line tested with device names containing CRLF sequences (`\r\n`), null bytes, and ASCII control characters (0x00–0x1F, 0x7F) to verify header injection prevention? [Testability, Spec §FR-007] <!-- Evaluator: PASS — Spec §FR-007 requires stripping of "CRLF sequences, null bytes, and other ASCII control characters (0x00–0x1F, 0x7F) before header insertion." Checks.py unit/integration test can inject these characters. -->
- [X] CHK030 Are non-string input types (integers, None, nested objects) for data fields tested to verify the coercion-to-string strategy via `str()` works correctly before escaping, with appropriate warnings logged? [Robustness, Plan §Error Handling] <!-- Evaluator: PASS — Plan §Error Handling for "HTML escape failure (non-string value)": "Coerce unexpected types via str() before escaping; log warning." Unit tests can pass None/int/list values and assert correct coercion. -->
- [X] CHK031 Does the test suite verify that template rendering completes within the 50ms performance budget per email, and is the 5-second asyncio.wait_for timeout tested? [Completeness, Plan §Performance Goals, Plan §Security] <!-- Evaluator: PASS — Plan §Performance Goals: "Template rendering ≤50ms per email"; Plan §Security: "Jinja2 template rendering has a 5-second timeout enforced via asyncio.wait_for." Both are testable with pytest-benchmark or timing assertions, and timeout via induced slow template. -->
- [X] CHK032 Are the color token substitutions (e.g., `{{ accent_color }}`, `{{ surface_color }}`) tested to verify they resolve to the correct hardcoded hex values in the rendered HTML output, confirming they are not sourced from user input? [Completeness, Spec §FR-010, Spec §Clarifications] <!-- Evaluator: PASS — Spec §FR-010 specifies exact hex values; Spec §Clarifications confirms color tokens are "hardcoded hex constants, never from user input." Template unit test can assert rendered output contains the expected hex color strings. -->

## Email Client & MIME Structure Validation

- [X] CHK033 Is the multipart/alternative MIME structure tested to verify both text/plain and text/html parts are present in the dispatched email for SMTP channels, with correct Content-Type headers? [Completeness, Spec §FR-006] <!-- Evaluator: PASS — Plan §Integration Points specifies Apprise SMTP plugin dispatch; Plan AD-005 documents Python email.mime via Apprise for MIME generation. Integration test can inspect dispatched MIME structure. -->
- [X] CHK034 Does the test suite verify the plain-text fallback body contains the same information fields (device name, device type, current version, latest version, source URL) as the HTML version? [Completeness, Spec §SC-002] <!-- Evaluator: PASS — Spec §SC-002: "Plain-text fallback email contains the same information as the HTML version." Plan §Project Structure lists email_update.txt as the plain-text fallback template. Unit test can compare rendered text vs. HTML content fields. -->
- [X] CHK035 Does the testing strategy address how success criterion SC-005 (no horizontal scrollbar, text ≥14px, no element overlap on Gmail/Apple Mail/Outlook at 375px/1440px) will be validated given that automated email client rendering tests are not feasible in CI? [Completeness, Spec §SC-005] <!-- Evaluator: RESOLVE — Spec §SC-005 defines measurable criteria; Plan §Risk Mitigation document manual testing in major email clients. No automated CI approach exists, which is acceptable given the constraints. The testing strategy acknowledges this via the Risk Mitigation entry "test in Gmail, Apple Mail, Outlook; accept minor cosmetic differences." -->
- [X] CHK036 Is the templates/ directory inclusion in the Docker image verified during CI build to prevent runtime template-not-found errors that would trigger the plain-text fallback for every notification? [Completeness, Plan HINT-004] <!-- Evaluator: RESOLVE — Plan HINT-004 instructs adding templates/ to the Dockerfile COPY directive. CI gates require "Docker image MUST build" (PI §CI Requirements), which fails if COPY references a missing directory. Runtime template loading is additionally covered by the template error fallback test (CHK016), so a template-not-found at runtime is both detectable and handled gracefully. -->

## Mock & Test Infrastructure

- [X] CHK037 Are the mock boundaries for integration tests clearly defined — capturing the Apprise notify() call parameters for SMTP (body_format=HTML) and Gotify (body_format=None) without actually dispatching to external services? [Clarity, Plan §Testing Strategy] <!-- Evaluator: PASS — Plan §Testing Strategy defines mock boundaries: "Apprise notify() call" for unit, "SMTP server, Gotify server" for integration. Plan HINT-003 specifies expected parameters: body_format=NotifyFormat.HTML for SMTP, body_format=None for Gotify. -->
- [X] CHK038 Does the test suite verify Jinja2 autoescape is enabled (autoescape=True) and no `|safe` filter overrides exist in the email template, confirming defense-in-depth against injection? [Testability, Spec §Clarifications, Plan HINT-002] <!-- Evaluator: PASS — Spec §Clarifications: "Jinja2 Environment is configured with autoescape=True; no |safe filter overrides are used in the email template." Plan HINT-002 specifies autoescape=True. Security test can confirm no |safe usage in template source. -->
