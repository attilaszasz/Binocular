# Epic Details: E001 — Application Skeleton & Container

- **Category**: TECHNICAL
- **Priority**: P1
- **Source**: {SAD:ADR-0001}{SAD:ADR-0002}{SAD:ADR-0008}{DOD:DDR-004}

## Scope
Establish the runnable FastAPI application skeleton with layered module structure, Pydantic Settings config, structlog logging, `/healthz` endpoint, and a non-root Docker container image with linuxserver-style PUID/PGID entrypoint via `su-exec`. The image exposes port 8000 and uses `python:3.13-slim` as the base.

## Traceability & Dependencies
- **Actors**: Operator
- **Key Entities**: None (infrastructure)
- **Depends on**: None
- **Dependency Contracts**: None
- **Depended on by**: E002, E003, E004, E005, E006, E007, E008, all subsequent
- **Produces (shared)**: App factory, config module, structlog setup, router aggregator, Dockerfile, compose.yaml, entrypoint.sh
- **Constraints**: Python 3.13+, non-root, PUID/PGID

## Acceptance Criteria
- [ ] `backend/src/binocular/app.py` defines a FastAPI app factory with lifespan
- [ ] `backend/src/binocular/config.py` uses Pydantic Settings for config + env vars
- [ ] `backend/src/binocular/logging.py` configures structlog with JSON output
- [ ] `/healthz` returns 200 OK
- [ ] Dockerfile builds a `python:3.13-slim` image with non-root execution
- [ ] `entrypoint.sh` supports PUID/PGID via `su-exec`
- [ ] `compose.yaml` with `/app/data` and `/app/modules` volumes
- [ ] `pyproject.toml` with all core dependencies
- [ ] `mypy --strict` passes

## Specify Input
- **Description**: Establish the runnable FastAPI app skeleton with config, logging, /healthz, non-root Docker container with PUID/PGID entrypoint, compose.yaml
- **Actors**: Operator
- **Key Entities**: None
- **Depends on Artifacts**: None
- **Constraints**: Python 3.13+, FastAPI, structlog, non-root, PUID/PGID, single port 8000

## Pipeline Hints
- skip_clarify, skip_checklist
