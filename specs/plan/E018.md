# Epic Details: E018 — Backup & Restore Operations

- **Category**: OPERATIONAL
- **Priority**: P2
- **Source**: {DOD:DDR-003}

## Scope
Implement scheduled live-safe SQLite backup via `VACUUM INTO` on a nightly APScheduler job. Provide a backup API endpoint and a restore runbook. Document the WAL-coupling caveat for raw volume copies.

## Traceability & Dependencies
- **Actors**: Operator
- **Key Entities**: None (operational)
- **Depends on**: E002 (DB connection), E013 (APScheduler)
- **Dependency Contracts**: E002 DB connection; E013 SchedulerService
- **Depended on by**: None
- **Produces (shared)**: BackupService, backup API route
- **Constraints**: VACUUM INTO for live-safe backups

## Acceptance Criteria
- [ ] Scheduled nightly backup job via APScheduler
- [ ] `VACUUM INTO` produces consistent single-file backup
- [ ] POST `/api/v1/backups/trigger` endpoint for manual backup
- [ ] Backup path configurable via env var
- [ ] WAL-coupling caveat documented
- [ ] Restore procedure documented in README
- [ ] Tests for backup creation

## Specify Input
- **Description**: Scheduled SQLite backup via VACUUM INTO, manual backup endpoint, restore runbook
- **Actors**: Operator
- **Key Entities**: None
- **Depends on Artifacts**: E002 DB, E013 scheduler
- **Constraints**: VACUUM INTO, live-safe

## Pipeline Hints
- skip_clarify, skip_checklist, lightweight
