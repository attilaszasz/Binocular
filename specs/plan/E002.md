# Epic Details: E002 — Data Layer & Migrations

- **Category**: TECHNICAL
- **Priority**: P1
- **Source**: {SAD:ADR-0004}{DOD:DDR-003}

## Scope
Implement the SQLite data layer with aiosqlite connection management (WAL, foreign_keys, busy_timeout pragmas), numbered migration runner (`schema_version` tracking, forward-only, idempotent), repository base class with raw SQL + parameter binding, and pre-migration backup gate.

## Traceability & Dependencies
- **Actors**: None (internal)
- **Key Entities**: schema_version
- **Depends on**: E001 (app lifespan)
- **Dependency Contracts**: E001 must provide the app factory lifespan hook
- **Depended on by**: E006, E007, E009, E010, E013, E014, E015, E018
- **Produces (shared)**: DB connection pool, migration runner, RepositoryBase, connection pragmas
- **Constraints**: No ORM, raw SQL, numbered migrations, WAL mode

## Acceptance Criteria
- [ ] `backend/src/binocular/db/connection.py` manages async connection pool with WAL, FK, busy_timeout
- [ ] `backend/src/binocular/db/migrations.py` runs numbered SQL files, tracks schema_version
- [ ] Pre-migration backup via `VACUUM INTO` before applying pending migrations
- [ ] `backend/src/binocular/repositories/base.py` provides parameterized query helpers
- [ ] Migrations applied automatically on app startup
- [ ] Tests validate migration runner idempotency and backup behavior

## Specify Input
- **Description**: SQLite data layer with aiosqlite, numbered migration runner, pre-migration backup, repository base with raw SQL
- **Actors**: None
- **Key Entities**: schema_version
- **Depends on Artifacts**: E001 app factory
- **Constraints**: No ORM, WAL mode, foreign_keys=ON, busy_timeout=5000

## Pipeline Hints
- skip_clarify, skip_checklist
