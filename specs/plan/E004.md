# Epic Details: E004 — Frontend SPA Shell

- **Category**: TECHNICAL
- **Priority**: P1
- **Source**: {SAD:ADR-0003}

## Scope
Create the React/Vite/Tailwind CSS v4 SPA with shadcn/ui as the canonical component library from day one. Implement collapsible sidebar navigation, dark mode with ThemeProvider, version display, and the component directory structure. Multi-stage Dockerfile stage compiles and copies dist/ into the backend image.

## Traceability & Dependencies
- **Actors**: Operator
- **Key Entities**: None (UI framework)
- **Depends on**: E001 (static file serving, SPA catch-all)
- **Dependency Contracts**: E001 must serve `static_dist/` via FastAPI StaticFiles
- **Depended on by**: E006, E009, E012, E015 (all frontend features)
- **Produces (shared)**: shadcn/ui primitives (button, card, select, badge, table, switch, tooltip, input, label), cn() utility, ThemeProvider, collapsible Sidebar, Header, NavItem, Brand, VersionDisplay, App.tsx with React Router
- **Constraints**: React 19, Tailwind CSS 4 CSS-first, shadcn/ui New York style

## Acceptance Criteria
- [ ] `frontend/src/` initialized with Vite, React 19, TypeScript, Tailwind CSS v4
- [ ] shadcn/ui components in `components/ui/`: button, card, select, badge, table, switch, tooltip, input, label
- [ ] `components/layout/`: Sidebar (collapsible), Header, NavItem, Brand, VersionDisplay
- [ ] ThemeProvider with system/light/dark modes, toggling `.dark` on `<html>`
- [ ] `cn()` helper via clsx + tailwind-merge in `lib/utils.ts`
- [ ] React Router with placeholder pages for inventory, modules, logs, settings
- [ ] Vite build outputs to `dist/`, Dockerfile Node stage copies to backend
- [ ] `VITE_APP_VERSION` injected from env at build time
- [ ] Responsive on mobile and desktop
- [ ] tsc and ESLint pass

## Specify Input
- **Description**: React/Vite/Tailwind v4 SPA shell with shadcn/ui from day one, collapsible sidebar nav, dark mode, version display, and component directory structure
- **Actors**: Operator
- **Key Entities**: None
- **Depends on Artifacts**: E001 static serving
- **Constraints**: React 19, Tailwind CSS 4 CSS-first, shadcn/ui New York, Zinc palette, blue primary accent

## Pipeline Hints
- skip_checklist
