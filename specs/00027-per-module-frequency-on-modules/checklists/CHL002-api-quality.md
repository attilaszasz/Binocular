# API Quality: Per-Module Frequency on Modules Page
**Created**: 2026-06-07 | **Feature**: [spec.md](../spec.md)

## Response Schema Correctness

- [X] CHK001 Does the ModuleResponse schema define `schedule` as nullable (`oneOf`: ModuleScheduleData | null) to distinguish "no schedule exists" from "schedule is disabled"? [Completeness] [Spec FR-005, FR-006; Plan AD-004; Contract ModuleResponse.schedule] <!-- Evaluator: Covered by contracts/openapi.yaml lines 110-113 — schedule: oneOf [ModuleScheduleData, null] -->
- [X] CHK002 Does ModuleScheduleData require both `enabled` and `intervalMinutes` to be present and non-null when a schedule row exists? [Completeness] [Spec FR-001; Contract ModuleScheduleData] <!-- Evaluator: Covered by contracts/openapi.yaml lines 118-127 — ModuleScheduleData required: [enabled, intervalMinutes], both non-nullable types -->
- [X] CHK003 Is `intervalMinutes` constrained to 1–10080 in both the response schema (ModuleScheduleData) and the request body schema (PUT), and is the same range used for server-side and client-side enforcement? [Consistency] [Spec FR-002; Contract ModuleScheduleData, PUT requestBody] <!-- Evaluator: Covered by contracts/openapi.yaml lines 63-64 (PUT minimum:1 maximum:10080) and lines 125-127 (ModuleScheduleData same range); spec.md FR-002 client-side validation; plan.md Error Handling server-side 422 backstop -->
- [X] CHK004 Does ModuleListResponse include `total`, `page`, and `pageSize` alongside the `modules` array so the client can render pagination controls without deriving counts? [Completeness] [Spec FR-006; Plan AD-003; Contract ModuleListResponse] <!-- Evaluator: Covered by contracts/openapi.yaml lines 128-145 — ModuleListResponse has modules, total, page, pageSize -->
- [X] CHK005 Is the `next_run_at` field in DeviceTypeScheduleResponse nullable, and is its null-value semantic (no upcoming run / disabled) documented for API consumers? [Clarity] [Spec FR-001; Plan AD-004; Contract DeviceTypeScheduleResponse] <!-- Evaluator: Resolved — added description to next_run_at in contracts/openapi.yaml clarifying null semantics (disabled, no upcoming trigger, or scheduler not yet initialized) -->
- [X] CHK006 Are `last_started_at`, `last_completed_at`, `last_success_at`, `last_failure_at`, `last_failure_reason`, and `last_skip_reason` all defined as nullable in DeviceTypeScheduleResponse, and do their null semantics align with the lifecycle of a device check? [Completeness] [Spec FR-003; Plan API Surface; Contract DeviceTypeScheduleResponse] <!-- Evaluator: Covered by contracts/openapi.yaml lines 163-184 — all six fields nullable; null = "has not occurred" is standard REST lifecycle semantics consistent with spec Edge Cases on check interruption behavior -->
- [X] CHK007 Is the `device_type` field in DeviceTypeScheduleResponse documented to clarify that its value derives from `modules.id` post-ADR-0009, given the parametric mismatch in the endpoint path? [Clarity] [Spec Assumptions; Plan Risk Mitigation] <!-- Evaluator: Resolved — added description to device_type in contracts/openapi.yaml clarifying it is semantically equivalent to module_id post-ADR-0009 -->
- [X] CHK008 Does ModuleResponse retain all pre-existing fields (`id`, `module_id`, `display_name`, `version`, `status`, `last_validated_at`) unmodified, so existing API consumers are not broken by the schema extension? [Consistency] [Spec FR-006; Contract ModuleResponse] <!-- Evaluator: Covered by contracts/openapi.yaml lines 92-109 — all pre-existing fields present with original types; plan.md AD-001 confirms additive-only change -->

## Pagination Correctness

- [X] CHK009 Are `page` and `pageSize` defined as optional query parameters with explicit defaults (page=1, pageSize=100) in the OpenAPI specification, and are these defaults documented for API consumers? [Completeness] [Spec FR-006; Plan HINT-004; Contract GET parameters] <!-- Evaluator: Covered by contracts/openapi.yaml lines 10-22 — page has default:1; pageSize defaults documented via description (formerly default:100, adjusted per CHK014/CHK029) -->
- [X] CHK010 Is `pageSize` capped at a maximum of 100 to prevent unbounded response payloads, and is the enforcement point (API gateway, handler, or repository) identified? [Completeness] [Spec FR-006; Contract pageSize.maximum] <!-- Evaluator: Resolved — added enforcement points to plan.md HINT-004: route handler validates via FastAPI/Pydantic schema constraints; repository applies validated values to SQL LIMIT/OFFSET -->
- [X] CHK011 Is `page` constrained to a minimum of 1 so that zero or negative page requests are rejected with a clear error? [Completeness] [Spec FR-006; Contract page.minimum] <!-- Evaluator: Resolved — added 422 response with ErrorResponse schema to GET endpoint in contracts/openapi.yaml for invalid query parameters -->
- [X] CHK012 Does the specification define the API response when the requested `page` exceeds the total number of available pages — should it return an empty modules array, a 404, or a 416 Range Not Satisfiable? [Clarity] [Spec FR-006; Plan AD-003] <!-- Evaluator: Resolved via autopilot — standard REST pagination convention: returns empty modules array with accurate total/page/pageSize metadata; documented in contracts/openapi.yaml 200 description and plan.md HINT-004 -->
- [X] CHK013 Is the `total` count defined with a minimum of 0 to handle the empty-collection case without special client-side logic? [Completeness] [Spec FR-006; Contract ModuleListResponse.total] <!-- Evaluator: Covered by contracts/openapi.yaml lines 136-138 — total: minimum: 0 -->
- [X] CHK014 Does the plan address backward compatibility for unpaginated clients by returning all modules when `pageSize` is omitted from the request? [Backward Compatibility] [Spec FR-006; Plan HINT-004, AD-003] <!-- Evaluator: Resolved — removed conflicting default:100 from pageSize in contracts/openapi.yaml; added description stating omitted pageSize returns all modules preserving pre-pagination behavior; plan.md HINT-004 explicitly mandates backward compatibility -->
- [X] CHK015 Are pagination metadata fields (`page`, `pageSize`, `total`) guaranteed in every 200 response, including edge cases like an empty module list or a single-module installation? [Consistency] [Spec FR-006; Contract ModuleListResponse] <!-- Evaluator: Resolved — added required: [modules, total, page, pageSize] to ModuleListResponse in contracts/openapi.yaml -->

## Error Semantics

- [X] CHK016 Does the API contract define a 404 response for PUT /api/v1/schedules/device-types/{deviceTypeId} when the module identified by the path parameter no longer exists? [Completeness] [Spec Edge Cases; Plan Error Handling; Contract PUT 404] <!-- Evaluator: Covered by contracts/openapi.yaml lines 72-77 — PUT 404 "Module not found" with ErrorResponse schema -->
- [X] CHK017 Does the API contract define a 422 response for the PUT endpoint when `intervalMinutes` is outside 1–10080 or `enabled` is not a boolean? [Completeness] [Spec FR-002; Plan Error Handling; Contract PUT 422] <!-- Evaluator: Covered by contracts/openapi.yaml lines 78-83 — PUT 422 "Validation error" with ErrorResponse schema -->
- [X] CHK018 Is the error response body structure (e.g., `{ "detail": "..." }`) documented consistently across both GET /api/v1/modules and PUT /api/v1/schedules/ endpoints, so error-handling logic is uniform? [Consistency] [Spec FR-002; Plan Error Handling] <!-- Evaluator: Resolved — added ErrorResponse schema (object with required detail:string) to contracts/openapi.yaml components; referenced in all GET 422/500 and PUT 404/422/500 responses -->
- [X] CHK019 Does the plan specify a 500 response for the partial-failure scenario where the DB write succeeds but the scheduler reschedule fails, and is a distinct error code or message defined for this case? [Completeness] [Spec Edge Cases; Plan Error Handling "Schedule upsert: scheduler reschedule fails"] <!-- Evaluator: Resolved — added 500 response to PUT endpoint in contracts/openapi.yaml with description "Database write succeeded but scheduler update failed"; plan.md Error Handling already specifies the error message and auto-retry behavior -->
- [X] CHK020 Does the spec define whether schedule-load failures for GET /api/v1/modules are per-module (null schedule + error flag) or request-level (500), and is this disambiguation present in the API contract? [Completeness] [Spec US1-4, Clarifications; Plan Error Handling "Module list load failure"] <!-- Evaluator: Resolved via autopilot — split into two scenarios: total module list load failure → 500; per-module schedule load failure → 200 with schedule_error:true + schedule:null on affected ModuleResponse. Added schedule_error boolean to ModuleResponse in contracts/openapi.yaml; added per-module failure row to plan.md Error Handling table -->
- [X] CHK021 Is the upsert semantic of the PUT endpoint clearly specified — does it return 200 (updated) vs. 201 (created), and is this consistent with the existing schedule API behavior described in the plan? [Clarity] [Spec FR-005; Plan "creates row on first write"] <!-- Evaluator: Covered by plan.md Assumptions "creates row on first write" (upsert); contracts/openapi.yaml PUT only defines 200 (no 201); consistent with idempotent PUT semantics -->
- [X] CHK022 Are concurrent-update conflict semantics defined in the API contract — is the resolution last-write-wins, optimistic-locking with ETag, or a 409 Conflict with the current server state? [Completeness] [Spec US2-9, Edge Cases; Plan Error Handling "Concurrent edit conflict"] <!-- Evaluator: Resolved via autopilot — clarified API uses last-write-wins (no ETag/409); frontend handles external change detection via TanStack Query cache invalidation. Updated plan.md Error Handling table with explicit last-write-wins semantics -->
- [X] CHK023 Does the specification define validation error responses when `page` or `pageSize` query parameters contain non-integer, negative, or out-of-range values? [Completeness] [Spec FR-002, FR-006; Contract GET parameters] <!-- Evaluator: Resolved — added 422 response with ErrorResponse schema to GET /api/v1/modules in contracts/openapi.yaml (same resolution as CHK011) -->

## Backward Compatibility

- [X] CHK024 Does the modified GET /api/v1/modules endpoint preserve all pre-existing response fields (`id`, `module_id`, `display_name`, `version`, `status`, `last_validated_at`) without deprecation or type changes? [Backward Compatibility] [Spec FR-006; Plan AD-001; Contract ModuleResponse] <!-- Evaluator: Covered by contracts/openapi.yaml lines 95-109 — all fields preserved with original types; plan.md AD-001 confirms additive change -->
- [X] CHK025 Is the new `schedule` field added as a nested, additive property rather than replacing or redefining any existing top-level field? [Backward Compatibility] [Spec FR-006; Contract ModuleResponse] <!-- Evaluator: Covered by contracts/openapi.yaml lines 110-113 — schedule is a new nested property using oneOf; no existing fields replaced or redefined -->
- [X] CHK026 Does the PUT /api/v1/schedules/device-types/{deviceTypeId} endpoint remain entirely unchanged in path, parameters, request body schema, and response schema, with only the semantic note that `deviceTypeId` now maps to `modules.id`? [Backward Compatibility] [Spec Assumptions; Plan API Surface; Contract PUT] <!-- Evaluator: Covered by contracts/openapi.yaml lines 42-89 — PUT endpoint structurally unchanged; plan.md API Surface table confirms "Reused unchanged"; semantic note documented in plan.md Risk Mitigation and contracts/openapi.yaml device_type description -->
- [X] CHK027 Are the existing POST /api/v1/modules and DELETE /api/v1/modules/{moduleId} endpoints explicitly affirmed as unchanged, so reviewers do not need to inspect them for unintended side effects? [Backward Compatibility] [Plan API Surface summary table] <!-- Evaluator: Covered by plan.md API Surface table — both POST and DELETE marked "Unchanged" -->
- [X] CHK028 Does the plan's decision to return `null` for modules with no schedule row (AD-004) avoid coupling the API response to client-side display defaults such as "24h" and "disabled"? [Backward Compatibility] [Spec FR-005; Plan AD-004] <!-- Evaluator: Covered by plan.md AD-004 rationale: "API returns null — Client owns display defaults; keeps API as faithful data layer; avoids coupling to UI conventions" -->
- [X] CHK029 Is backward compatibility maintained for callers that omit the `pageSize` query parameter — does the API return all modules (not just a default page slice), preserving the pre-pagination behavior? [Backward Compatibility] [Plan HINT-004] <!-- Evaluator: Resolved — removed conflicting default:100 from pageSize in contracts/openapi.yaml; added description: "When omitted, returns all modules preserving pre-pagination backward compatibility." Plan HINT-004 mandates this. (Same resolution as CHK014) -->

## Contract Adherence

- [X] CHK030 Does the OpenAPI contract accurately reflect all request and response fields described in the spec and plan, with no undocumented fields, missing required properties, or schema drift? [Consistency] [Spec FR-001–FR-006; Plan API Surface; Contract] <!-- Evaluator: Covered — all spec/plan fields present in contracts/openapi.yaml; resolved gaps (error response schemas, required arrays, schedule_error field) addressed via CHK005/CHK007/CHK015/CHK018/CHK019/CHK020 -->
- [X] CHK031 Are the `status` enum values (`valid`, `invalid`, `unvalidated`) in ModuleResponse consistent with the module validation taxonomy used throughout the system? [Consistency] [Spec module validation lifecycle; Contract ModuleResponse.status] <!-- Evaluator: Covered by contracts/openapi.yaml lines 103-105 — enum: [valid, invalid, unvalidated]; plan.md API Surface table references this taxonomy -->
- [X] CHK032 Does the contract use `oneOf` (ModuleScheduleData | null) for the `schedule` field rather than optional properties within a flat object, correctly modeling the "has schedule" vs. "no schedule" distinction? [Contract Adherence] [Spec FR-005; Plan AD-004; Contract ModuleResponse.schedule] <!-- Evaluator: Covered by contracts/openapi.yaml lines 110-113 — oneOf: [ModuleScheduleData, null]; correctly models binary distinction per plan.md AD-004 -->
- [X] CHK033 Are all date-time fields (`last_validated_at`, `next_run_at`, `last_started_at`, `last_completed_at`, `last_success_at`, `last_failure_at`) formatted as ISO 8601 strings consistently across all schemas in the contract? [Consistency] [Spec US1-3; Plan Testing Strategy; Contract] <!-- Evaluator: Covered by contracts/openapi.yaml — all date-time fields use type: string, format: date-time consistently across ModuleResponse and DeviceTypeScheduleResponse -->
- [X] CHK034 Does the contract specify `application/json` as the content type for the PUT request body and all 200 response bodies, consistent with the existing API surface? [Contract Adherence] [Plan API Surface; Contract PUT requestBody.content] <!-- Evaluator: Covered by contracts/openapi.yaml — GET 200 (line 27), PUT 200 (line 69), PUT requestBody (line 54) all use application/json -->
- [X] CHK035 Is `device_type_id` in DeviceTypeScheduleResponse documented as an integer, matching the `deviceTypeId` path parameter type and the `modules.id` key type in the database? [Consistency] [Spec Assumptions; Plan Risk Mitigation; Contract] <!-- Evaluator: Covered by contracts/openapi.yaml — device_type_id: type: integer (line 150); deviceTypeId path param: type: integer (line 50); plan.md HINT-002 confirms modules.id = device_type_schedules.device_type_id -->
- [X] CHK036 Does the API contract for the modified GET endpoint include all three pagination query parameters (`page`, `pageSize`) and all three pagination response fields (`total`, `page`, `pageSize`), forming a self-documenting pagination contract? [Completeness] [Spec FR-006; Contract GET, ModuleListResponse] <!-- Evaluator: Covered by contracts/openapi.yaml — GET query params page + pageSize; ModuleListResponse has total + page + pageSize alongside modules array -->
- [X] CHK037 Are the `required` arrays in the OpenAPI schemas (ModuleScheduleData: `[enabled, intervalMinutes]`; PUT request body: `[enabled, intervalMinutes]`) consistent with the spec's assertion that both pieces of data are mandatory for schedule configuration? [Consistency] [Spec FR-001, FR-002, FR-003; Contract] <!-- Evaluator: Covered by contracts/openapi.yaml — ModuleScheduleData required: [enabled, intervalMinutes] (line 120); PUT requestBody required: [enabled, intervalMinutes] (line 57); consistent with spec FR-001/FR-002/FR-003 -->
- [X] CHK038 Does the `oneOf` constraint on the `schedule` field need an explicit discriminator property (e.g., `"type": "ScheduleData"`), or is the distinction between an object and `null` sufficient for all downstream code generators and validators? [Clarity] [Spec FR-005; Plan AD-004; Contract ModuleResponse.schedule] <!-- Evaluator: Covered by contracts/openapi.yaml — type-based oneOf [object, null] is sufficient for standard Python/Pydantic and TypeScript code generators; adding a discriminator would unnecessarily change the response contract -->
