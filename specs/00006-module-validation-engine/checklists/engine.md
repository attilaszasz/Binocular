# Engine Quality: Module Validation Engine
**Created**: 2026-03-04 | **Feature**: [spec.md](../spec.md)

## Static Analysis Correctness

- [X] CHK001 Does the spec define what constitutes a "top-level definition" for the required function, excluding class methods and nested functions? [Clarity, Spec §Edge Cases] <!-- Evaluator: Covered by spec.md §Edge Cases — "Check function defined inside a class or nested function: reported as missing (only top-level definitions count)" -->
- [X] CHK002 Are the required parameter names (`url`, `model`, `http_client`) and their exact order explicitly specified in the contract definition? [Completeness, Spec §FR-002] <!-- Evaluator: Covered by spec.md §FR-002 — "check_firmware(url, model, http_client)" and 00005 loader _REQUIRED_PARAMS -->
- [X] CHK003 Does FR-001 clarify whether the static validator should reject files with valid syntax but additional unexpected top-level functions beyond `check_firmware`? [Clarity, Spec §FR-001] <!-- Evaluator: Covered by spec.md §FR-001 — validates required elements exist; no requirement to reject extras -->
- [X] CHK004 Is the behavior specified for a module where `check_firmware` exists but has extra parameters beyond the required three (e.g., `*args`, `**kwargs`, default params)? [Completeness, Spec §FR-002] <!-- Evaluator: Resolved — added clarification to spec.md §Clarifications: exact 3-param match required, extras produce INVALID_SIGNATURE -->
- [X] CHK005 Does the spec define how the AST analysis handles `AugAssign` or annotated assignments (e.g., `MODULE_VERSION: str = "1.0"`) for manifest constant detection? [Clarity, Spec §FR-001] <!-- Evaluator: Resolved — added to spec.md §Clarifications and plan.md §AD-1: AnnAssign nodes accepted alongside Assign -->
- [X] CHK006 Is it specified whether manifest constants must be string literals at the AST level, or whether any assignment (e.g., computed values, function calls) is acceptable? [Completeness, Spec §FR-002] <!-- Evaluator: Resolved per user — existence-only check at AST level; type verification deferred to runtime phase. Added to spec.md §Clarifications -->
- [X] CHK007 Does FR-003 explicitly require that all errors are collected even when the first error is a syntax error (which prevents further AST walking)? [Completeness, Spec §FR-003] <!-- Evaluator: Resolved — added clarification to spec.md: syntax errors prevent AST walking, only SYNTAX_ERROR reported; FR-003 applies within parseable files -->
- [X] CHK008 Is the "location" information in error messages defined — does it include line number, column, or just the defect category? [Clarity, Spec §SC-002] <!-- Evaluator: Resolved per user — line number when available from AST node; syntax errors include line+column from SyntaxError. Added to spec.md §Clarifications -->
- [X] CHK009 Does the plan specify that `ast.parse()` should use `type_comments=False` to avoid false failures from PEP 484 comment syntax? [Completeness, Plan §AD-1] <!-- Evaluator: Resolved — added type_comments=False to plan.md §AD-1 -->

## Error Code Completeness

- [X] CHK010 Does the closed enum cover all failure modes described in the edge cases section (binary data, 0-byte file, non-UTF-8, oversized, nested function, `sys.exit()`, unreachable URL, `ImportError`)? [Completeness, Spec §Edge Cases + Key Entities] <!-- Evaluator: Resolved — added 0-byte → ENCODING_ERROR mapping to spec.md §Edge Cases and §Clarifications. All edge cases now map to existing codes. -->
- [X] CHK011 Is there a mapping from each edge case to a specific `ValidationErrorCode`, with no ambiguous overlaps between codes? [Consistency, Spec §Key Entities] <!-- Evaluator: Covered — each edge case maps to one code: binary/non-UTF-8/0-byte→ENCODING_ERROR, oversized→FILE_TOO_LARGE, nested fn→MISSING_FUNCTION, sys.exit/unreachable/ImportError→RUNTIME_EXCEPTION, invalid return→INVALID_RETURN_VALUE -->
- [X] CHK012 Does the spec define which error code applies to a 0-byte file — is it `ENCODING_ERROR`, `FILE_TOO_LARGE`, or an implicit "empty file" failure? [Clarity, Spec §Edge Cases] <!-- Evaluator: Resolved — added to spec.md §Edge Cases: 0-byte → ENCODING_ERROR, and to §Clarifications -->
- [X] CHK013 Does the spec clarify whether `ImportError` during runtime produces `RUNTIME_EXCEPTION` or needs a distinct code? [Clarity, Spec §Edge Cases] <!-- Evaluator: Covered by spec.md §Edge Cases — "missing dependency (ImportError): runtime fails with error details" maps to RUNTIME_EXCEPTION -->
- [X] CHK014 Is the distinction between `RUNTIME_EXCEPTION` and `INVALID_RETURN_VALUE` unambiguous — what if the function returns a dict missing `latest_version`? [Clarity, Spec §FR-006 + Key Entities] <!-- Evaluator: Covered — FR-006 validates return value via CheckResult.model_validate(); Pydantic ValidationError → INVALID_RETURN_VALUE. Exceptions during execution → RUNTIME_EXCEPTION. -->
- [X] CHK015 Does the spec define the error code for a module whose `check_firmware` is callable but is a class instance (not a function), caught at AST level? [Completeness, Spec §FR-001] <!-- Evaluator: Covered — AST walks FunctionDef nodes only; a class with __call__ is ClassDef → reported as MISSING_FUNCTION -->
- [X] CHK016 Are the human-readable message requirements defined — max length, required content (code + location), localization constraints? [Completeness, Spec §Key Entities] <!-- Evaluator: Covered by data-model.md — "message: str (human-readable, ≤500 chars)" -->
- [X] CHK017 Does the plan confirm that `ValidationErrorCode` is implemented as a Python `Literal` type (or `StrEnum`) compatible with `mypy --strict`? [Consistency, Plan §AD-6 + Instructions §IV] <!-- Evaluator: Covered by plan.md §Instructions Check IV — "ValidationErrorCode is a strict Literal type" -->

## Loader Integration Safety

- [X] CHK018 Does FR-012 specify the exact integration point — when during `loader.scan()` the static validator is invoked relative to hash comparison and import? [Completeness, Spec §FR-012] <!-- Evaluator: Covered by plan.md §AD-3 — "scan() → validate_static() → if fail, register inactive → if pass, _safe_import() → register active" -->
- [X] CHK019 Does the plan define what happens to the loader's existing `_validate_module()` method — removed, deprecated, or retained as a secondary check? [Clarity, Plan §AD-3] <!-- Evaluator: Resolved — updated plan.md §AD-3: "_validate_module() which is removed" -->
- [X] CHK020 Is the error-mapping strategy from `ValidationError` objects to the loader's `last_error: str` field explicitly defined? [Completeness, Plan §Engine Layer] <!-- Evaluator: Resolved — updated plan.md §AD-3: errors "joined via '; ' separator for last_error field" -->
- [X] CHK021 Does the plan address backward compatibility — will modules previously registered as active be re-validated with the new static validator on next scan? [Completeness, Plan §AD-3] <!-- Evaluator: Covered — loader.scan() uses hash comparison; if file hash changed → re-validate with new static validator; if unchanged → skip (existing behavior) -->
- [X] CHK022 Is the behavior defined when the static validator returns errors for a module that is currently active and in-use by the executor? [Completeness, Spec §FR-012] <!-- Evaluator: Covered by existing loader.scan() behavior — sets is_active=false and removes from registry; executor lookups return None -->
- [X] CHK023 Does the plan specify that the loader integration preserves the existing hash-based skip optimization (skip re-import when file unchanged)? [Consistency, Plan §AD-3] <!-- Evaluator: Covered — AD-3 modifies the scan flow after hash comparison; the hash check remains in loader.scan() existing code -->
- [X] CHK024 Is it clear whether the static validator replaces both the `_safe_import` error handling AND `_validate_module`, or only the structural checks? [Clarity, Plan §AD-3] <!-- Evaluator: Covered by plan.md §AD-3 — "replacing the post-import _validate_module() for structural checks"; _safe_import error handling is separate -->

## Runtime Validation Completeness

- [X] CHK025 Does FR-007 specify whether `asyncio.CancelledError` is treated the same as `TimeoutError`, or if cancellation has a distinct behavior? [Completeness, Spec §FR-007] <!-- Evaluator: Covered — V1 single-user product; no external cancellation source. asyncio.wait_for() raises TimeoutError. CancelledError is not a relevant scenario. -->
- [X] CHK026 Is the runtime timeout value sourced from a parameter to `validate()`, or does the engine read it from `AppConfig` (introducing a DB dependency that violates FR-011)? [Consistency, Spec §FR-007 + FR-011] <!-- Evaluator: Resolved — updated plan.md §AD-7: "validate() and validate_runtime() accept an explicit timeout_seconds: int = 30 parameter — no database read" -->
- [X] CHK027 Does the plan confirm that the runtime phase uses a fresh module import (not the loader's cached registry) to avoid stale-state issues? [Completeness, Plan §AD-7] <!-- Evaluator: Covered by plan.md §AD-7 — "Runtime loads via importlib" (fresh import, not cached registry) -->
- [X] CHK028 Is the behavior specified for a module that passes static validation but fails to import at runtime (e.g., imports an unavailable third-party package)? [Completeness, Spec §Edge Cases] <!-- Evaluator: Covered by spec.md §FR-007 — "catch all exceptions (including SystemExit) during execution" and §Edge Cases "missing dependency (ImportError): runtime fails with error details" -->
- [X] CHK029 Does the plan define whether the HTTP client passed to runtime validation is closed/cleaned up by the engine or by the caller? [Clarity, Plan §AD-7] <!-- Evaluator: Covered by quickstart.md §3 — caller manages lifecycle with try/finally client.close() -->
- [X] CHK030 Is the `elapsed_seconds` measurement defined — wall-clock time from start of import to return, or only the `check_firmware()` invocation? [Clarity, Spec §FR-008] <!-- Evaluator: Resolved — added clarification to spec.md: wall-clock time for entire runtime phase (import through check_firmware() return) -->

## Structured Result Completeness

- [X] CHK031 Does FR-008 define the `ValidationResult` shape precisely enough that two independent implementations would produce structurally identical outputs for the same input? [Testability, Spec §FR-008] <!-- Evaluator: Covered by spec.md §FR-008 + data-model.md entity table with exact field types and constraints -->
- [X] CHK032 Is the `overall_verdict` derivation rule explicitly stated — is it a computed property or a stored field, and what happens if runtime is skipped? [Clarity, Spec §FR-008 + Key Entities] <!-- Evaluator: Covered by data-model.md — "derived: pass only when static_phase.status == 'pass' AND runtime_phase.status == 'pass'. If runtime is skipped, verdict is fail." -->
- [X] CHK033 Does the data model define whether `PhaseResult.errors` is an empty list (not `None`) when the phase passes? [Completeness, Data Model] <!-- Evaluator: Covered by data-model.md — "errors: list[ValidationError] (empty on pass)" -->
- [X] CHK034 Are the optional fields (`version_found`, `elapsed_seconds`) defined as `None` when runtime is skipped vs. when runtime fails? [Clarity, Spec §FR-008] <!-- Evaluator: Covered by data-model.md — "populated only for a passing runtime phase" → None for both skipped and failed -->

## Standalone Usability (FR-009)

- [X] CHK035 Does FR-009 define the public API surface of the engine — which functions/classes are importable by standalone callers? [Completeness, Spec §FR-009] <!-- Evaluator: Covered by plan.md §Project Structure — exports validate_static(), validate_runtime(), validate() via __init__.py -->
- [X] CHK036 Is the `create_http_client()` factory specified with enough detail (parameters, defaults, return type) for a caller to use it without reading source code? [Clarity, Spec §FR-009 + Plan §AD-4] <!-- Evaluator: Covered by existing http_client.py — fully documented with type-hinted params, defaults, and return type -->
- [X] CHK037 Does the plan define whether `validate()` is sync or async, and whether standalone callers need an event loop to use it? [Clarity, Plan §AD-7] <!-- Evaluator: Covered by quickstart.md §3 — shows "await validate()" inside asyncio.run(), confirming async -->
- [X] CHK038 Is it specified that the engine has zero required dependencies beyond what the host application already installs (no new pip packages)? [Consistency, Plan §Technical Context] <!-- Evaluator: Covered by plan.md §Technical Context — "stdlib: ast, importlib" plus existing httpx/Pydantic -->

## Cross-Feature Consistency

- [X] CHK039 Does the plan's contract definition (`check_firmware(url, model, http_client)` + `MODULE_VERSION` + `SUPPORTED_DEVICE_TYPE`) exactly match the 00005 loader's `_REQUIRED_PARAMS` and `_REQUIRED_CONSTANTS`? [Consistency, Plan §AD-1 + 00005 loader.py] <!-- Evaluator: Covered — spec FR-002 matches 00005 loader.py _REQUIRED_PARAMS=("url","model","http_client"), _REQUIRED_CONSTANTS=("MODULE_VERSION","SUPPORTED_DEVICE_TYPE") -->
- [X] CHK040 Is the relationship between the engine's `ValidationError` model and the existing API `ErrorCode` values (from 00005's schemas) clarified — are they separate type hierarchies? [Clarity, Plan §Layer Map] <!-- Evaluator: Covered by plan.md §No Changes — "No new endpoints", no changes to API schemas; ValidationError is in models/validation_result.py, separate from api/schemas/errors.py -->
